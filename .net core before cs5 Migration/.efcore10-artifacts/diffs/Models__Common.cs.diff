diff --git "a/C:\\Quick Soft 10-06-2026\\.efcore10-artifacts\\.efcore10-bak\\Models__Common.cs" "b/C:\\Quick Soft 10-06-2026\\QuickNetCore\\Models\\Common.cs"
index 1b18210..f956ad2 100644
--- "a/C:\\Quick Soft 10-06-2026\\.efcore10-artifacts\\.efcore10-bak\\Models__Common.cs"	
+++ "b/C:\\Quick Soft 10-06-2026\\QuickNetCore\\Models\\Common.cs"	
@@ -1142,9 +1142,9 @@ namespace QuickSoft.Models
                 var Reciept = (from a in db.Receipts
                                join b in db.Accountss on a.PayTo equals b.AccountsID
                                join c in db.Accountss on a.PayFrom equals c.AccountsID
-                               let bb = db.AccountsTransactions.Where(at => at.Purpose == "Receipt" && at.reference == a.ReceiptId).FirstOrDefault()
+                               let bb = db.AccountsTransactions.Any(at => at.Purpose == "Receipt" && at.reference == a.ReceiptId)
                                where (to == null || EF.Functions.DateDiffDay(a.Date, tdate) > 0) && (a.PayFrom == id) &&
-                               (pdc != true || bb != null || a.editable == choice.No)
+                               (pdc != true || bb || a.editable == choice.No)
                                select new
                                {
                                    Credit = (decimal?)a.Paying,
@@ -1152,9 +1152,9 @@ namespace QuickSoft.Models
                 var Payment = (from a in db.Payments
                                join b in db.Accountss on a.PayFrom equals b.AccountsID
                                join c in db.Accountss on a.PayTo equals c.AccountsID
-                               let bb = db.AccountsTransactions.Where(at => at.Purpose == "Payment" && at.reference == a.PaymentId).FirstOrDefault()
+                               let bb = db.AccountsTransactions.Any(at => at.Purpose == "Payment" && at.reference == a.PaymentId)
                                where (to == null || EF.Functions.DateDiffDay(a.Date, tdate) > 0) && a.PayTo == id &&
-                               (pdc != true || bb != null || a.editable == choice.No)
+                               (pdc != true || bb || a.editable == choice.No)
                                select new
                                {
                                    Debit = (decimal?)a.Paying
@@ -1220,9 +1220,9 @@ namespace QuickSoft.Models
                 var Reciept = (from a in db.Receipts
                                join b in db.Accountss on a.PayTo equals b.AccountsID
                                join c in db.Accountss on a.PayFrom equals c.AccountsID
-                               let bb = db.AccountsTransactions.Where(at => at.Purpose == "Receipt" && at.reference == a.ReceiptId).FirstOrDefault()
+                               let bb = db.AccountsTransactions.Any(at => at.Purpose == "Receipt" && at.reference == a.ReceiptId)
                                where (to == null || EF.Functions.DateDiffDay(a.Date, tdate) > 0) && a.PayFrom == id &&
-                               (pdc != true || bb != null || a.editable == choice.No)
+                               (pdc != true || bb || a.editable == choice.No)
                                select new
                                {
                                    Credit = (decimal?)a.Paying,
@@ -1230,9 +1230,9 @@ namespace QuickSoft.Models
                 var Payment = (from a in db.Payments
                                join b in db.Accountss on a.PayFrom equals b.AccountsID
                                join c in db.Accountss on a.PayTo equals c.AccountsID
-                               let bb = db.AccountsTransactions.Where(at => at.Purpose == "Payment" && at.reference == a.PaymentId).FirstOrDefault()
+                               let bb = db.AccountsTransactions.Any(at => at.Purpose == "Payment" && at.reference == a.PaymentId)
                                where (to == null || EF.Functions.DateDiffDay(a.Date, tdate) > 0) && a.PayTo == id &&
-                               (pdc != true || bb != null || a.editable == choice.No)
+                               (pdc != true || bb || a.editable == choice.No)
                                select new
                                {
                                    Debit = (decimal?)a.Paying,
@@ -1285,9 +1285,9 @@ namespace QuickSoft.Models
                 var Reciept = (from a in db.Receipts
                                join b in db.Accountss on a.PayTo equals b.AccountsID
                                join c in db.Accountss on a.PayFrom equals c.AccountsID
-                               let bb = db.AccountsTransactions.Where(at => at.Purpose == "Receipt" && at.reference == a.ReceiptId).FirstOrDefault()
+                               let bb = db.AccountsTransactions.Any(at => at.Purpose == "Receipt" && at.reference == a.ReceiptId)
                                where (to == null || EF.Functions.DateDiffDay(a.Date, tdate) > 0) && a.PayFrom == id &&
-                               (pdc != true || bb != null || a.editable == choice.No)
+                               (pdc != true || bb || a.editable == choice.No)
                                select new
                                {
                                    Credit = (decimal?)a.Paying,
@@ -1295,9 +1295,9 @@ namespace QuickSoft.Models
                 var Payment = (from a in db.Payments
                                join b in db.Accountss on a.PayFrom equals b.AccountsID
                                join c in db.Accountss on a.PayTo equals c.AccountsID
-                               let bb = db.AccountsTransactions.Where(at => at.Purpose == "Payment" && at.reference == a.PaymentId).FirstOrDefault()
+                               let bb = db.AccountsTransactions.Any(at => at.Purpose == "Payment" && at.reference == a.PaymentId)
                                where (to == null || EF.Functions.DateDiffDay(a.Date, tdate) > 0) && a.PayTo == id &&
-                               (pdc != true || bb != null || a.editable == choice.No)
+                               (pdc != true || bb || a.editable == choice.No)
                                select new
                                {
                                    Debit = (decimal?)a.Paying,
@@ -1350,9 +1350,9 @@ namespace QuickSoft.Models
                                join c in db.Accountss on a.PayTo equals c.AccountsID
                                join d in db.SalesEntrys on a.Reference equals d.SalesEntryId into sale
                                from d in sale.DefaultIfEmpty()
-                               let bb = db.AccountsTransactions.Where(at => at.Purpose == "Receipt" && at.reference == a.ReceiptId).FirstOrDefault()
+                               let bb = db.AccountsTransactions.Any(at => at.Purpose == "Receipt" && at.reference == a.ReceiptId)
                                where (to == null || EF.Functions.DateDiffDay(a.Date, tdate) > 0) && a.PayTo == id &&
-                               (pdc != true || bb != null || a.editable == choice.No)
+                               (pdc != true || bb || a.editable == choice.No)
                                select new
                                {
                                    Debit = (decimal?)a.Paying,
@@ -1362,9 +1362,9 @@ namespace QuickSoft.Models
                                join c in db.Accountss on a.PayFrom equals c.AccountsID
                                join d in db.PurchaseEntrys on a.Reference equals d.PurchaseEntryId into pur
                                from d in pur.DefaultIfEmpty()
-                               let bb = db.AccountsTransactions.Where(at => at.Purpose == "Payment" && at.reference == a.PaymentId).FirstOrDefault()
+                               let bb = db.AccountsTransactions.Any(at => at.Purpose == "Payment" && at.reference == a.PaymentId)
                                where (to == null || EF.Functions.DateDiffDay(a.Date, tdate) > 0) && a.PayFrom == id &&
-                               (pdc != true || bb != null || a.editable == choice.No)
+                               (pdc != true || bb || a.editable == choice.No)
                                select new
                                {
                                    Credit = (decimal?)a.Paying
@@ -8587,8 +8587,8 @@ on PEPayments.PurchaseEntry = q3.PurchaseEntryId";
                           Cashier = e.FirstName,
                           Discount = a.SEDiscount,
                           GrandTotal = a.SEGrandTotal,
-                          Paid = d.SEPaidAmount,
-                          Balance = a.SEGrandTotal - d.SEPaidAmount,
+                          Paid = d != null ? d.SEPaidAmount : 0,
+                          Balance = a.SEGrandTotal - (d != null ? d.SEPaidAmount : 0),
                           SubTotal = a.SESubTotal,
                           TaxAmount = a.SETaxAmount,
                           Address = b.Addres.Replace(System.Environment.NewLine, "<br/>"),
@@ -9753,7 +9753,7 @@ on PEPayments.PurchaseEntry = q3.PurchaseEntryId";
                           Mobile = k.MobileNum,// d.Mobile,
                           TRN = c.TaxRegNo,
                           PTax = b.SOTax,
-                          validity = (DateTime.Now <= DbFunctionsCompat.AddDays(b.SODate, (b.SOValidity == null) ? 0 : b.SOValidity + 1)) ? "Active" : "Expired",
+                          validity = (DateTime.Now <= b.SODate.AddDays((b.SOValidity == null) ? 0 : (b.SOValidity.Value + 1))) ? "Active" : "Expired",
                           BillId = b.SONo,
                           Remarks = b.Remarks,
                           chkCode = PrintCode,
@@ -10814,7 +10814,7 @@ on PEPayments.PurchaseEntry = q3.PurchaseEntryId";
                           TRN = i.TRN,
                           PTax = b.PQuotTax,
                           Remarks = b.Remarks,
-                          validity = (DateTime.Now <= DbFunctionsCompat.AddDays(b.PQuotDate, (b.PQuotValidity == null) ? 0 : b.PQuotValidity + 1)) ? "Active" : "Expired",
+                          validity = (DateTime.Now <= b.PQuotDate.AddDays((b.PQuotValidity == null) ? 0 : (b.PQuotValidity.Value + 1))) ? "Active" : "Expired",
                           BillId = b.PQuotNo,
                           chkCode = PrintCode,
                           TimeOut = TOut,
@@ -11941,7 +11941,7 @@ on PEPayments.PurchaseEntry = q3.PurchaseEntryId";
                           TRN = h.TRN,
                           PTax = b.POTax,
                           Remarks = b.Remarks,
-                          validity = (DateTime.Now <= DbFunctionsCompat.AddDays(b.PODate, (b.POValidity == null) ? 0 : b.POValidity + 1)) ? "Active" : "Expired",
+                          validity = (DateTime.Now <= b.PODate.AddDays((b.POValidity == null) ? 0 : (b.POValidity.Value + 1))) ? "Active" : "Expired",
                           BillId = b.PONo,
                           paytype = (b.SupplierType == SupplierType.CashSale ? "Cash" : "Credit"),
                           //CreditPeriod = c.CreditPeriod,
