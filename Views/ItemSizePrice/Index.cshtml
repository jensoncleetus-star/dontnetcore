@model QuickSoft.Models.itemsizeprice
@{
    ViewBag.Title       =   "Item Size Price List";
    ViewBag.SubTitle    =   "Item Size Price";
    ViewBag.Active      =   "Master";
    string actData      =   "";
}
<div class="box box-info">
    <div class="box-header with-border">
        <h3 class="box-title">Item Size Price List</h3>
        <div class="pull-right">
            <i class="ace-icon fa fa-hand-o-right icon-animated-hand-pointer blue"></i>
            <a class="btn btn-info no-border modal-create" href="/ItemSizePrice/Create">
                <i class="glyphicon glyphicon-plus"></i> Create Item Size Price
            </a>
        </div>
    </div>   
    <div class="box-body">
        <table class="table table-bordered table-hover" id="GroupTable">
            <thead>
                <tr>
                    <td data-priority="1">#</td>
                    <td data-priority="2">Item Name</td>
                    <td data-priority="2">Item Size</td>
                    <td data-priority="5" >Price</td>
                    <td data-priority="3">Action</td>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>
</div>
@Html.Partial("../Common/_ModalCreate")
@Html.Partial("../Common/_ModalEdit")
@Html.Partial("../Common/_ModalDelete")
@section scripts{
    @Scripts.Render("~/js/jqueryval")

<script type="text/javascript">

            var oTable = $('#GroupTable').DataTable({
                "processing": true, // for show progress bar
                "serverSide": true, // for process server side
                "filter": true, // this is for disable filter (search box)
                "orderMulti": false, // for disable multiple column at once
                "bStateSave": true,
                "pageLength": 50,
                responsive: true,
                "ajax": {
                    "url": '/ItemSizePrice/GetData',
                    "data": function (data) {
                    },
                    "type": "POST",
                    "datatype": "json"
                },
                "columns": [
                    {
                        "data": "sizepriceid", "name": "SizePriceId",
                        "render": function (data, type, row, meta) {
                            return meta.row + meta.settings._iDisplayStart + 1;
                        }
                    },
                    { "data": "ItemName", "name": "ItemName" },
                    { "data": "ItemSizeName", "name": "ItemSizeName" },
                    { "data": "price", "name": "price"},
                    {
                        "name": "sizepriceid", "data": function (data) {
                            var rest = "";
                            @{
                                actData += "<a class=\"modal-edit\" href=\"/ItemSizePrice/Edit/'+ data.sizepriceid +'\" title=\"Edit Record\"><i class=\"fa fa-lg fa-edit\" ></i></a> ";
                                actData += "<a class=\"modal-delete\" href=\"/ItemSizePrice/Delete/'+ data.sizepriceid +'\" title=\"Delete Record\"><i class=\"fa fa-lg fa-trash\" ></i></a> ";
                            }
                            return rest = '@Html.Raw(actData)';
                        }
                    }
                ]
            });          

            $('div').on('click', '.modal-create', function (e) {
                e.preventDefault();
                var url = $(this).attr('href');
                modalshow(url, '#modal-create');
            });
            $('#modal-create').on('submit', '#createform', function (e) {
                e.preventDefault();
                var url = $('#createform')[0].action;
                var data = $('#createform').serialize();
                createajax(url, data, '#modal-create');
                oTable.draw();
            });

            $('div').on('click', '.modal-edit', function (e) {
                e.preventDefault();
                var url = $(this).attr('href');
                modalshow(url, '#modal-edit');
            });
            $('#modal-edit').on('submit', '#editform', function (e) {
                e.preventDefault();
                var url = $('#editform')[0].action;
                var data = $('#editform').serialize();
                createajax(url, data, '#modal-edit');
                oTable.draw();
            });

            //$('.table').on('click', '.modal-delete', function (e) {
            //    e.preventDefault();
            //    var url = $(this).attr('href');
            //    modalshow(url, '#modal-delete');
            //});
            //$('#modal-delete').on('submit', '#deleteform', function (e) {
            //    e.preventDefault();
            //    var url = $('#deleteform')[0].action;
            //    var data = $('#deleteform').serialize();
            //    createajax(url, data, '#modal-delete');
            //    oTable.draw();
            //});
                
            $.fn.modal.Constructor.prototype.enforceFocus = function () { };
       
</script>
}

