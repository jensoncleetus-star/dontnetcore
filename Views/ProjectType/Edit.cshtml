@model QuickSoft.Models.ProjectType
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4>Edit Project Type</h4>
</div>
<div class="modal-body">
    @using (Html.BeginForm("Edit", "ProjectType", FormMethod.Post, new { id = "editform" }))
            {
        <script type="text/javascript">
            $('form#editform').removeData("validator");
            $('form#editform').removeData("unobtrusiveValidation");
            $.validator.unobtrusive.parse($('form#editform'));
        </script>
        @Html.ValidationSummary("", new { @class = "alert alert-danger" })
        @Html.HiddenFor(model => model.ProjectTypeID)

        <div class="row">
            <div class="col-md-12 form-group">
                @Html.LabelFor(model => model.TypeName, htmlAttributes: new { @class = "control-label" }) *
                @Html.EditorFor(model => model.TypeName, new { htmlAttributes = new { @class = "form-control", tabindex = 1, autofocus = "autofocus" } })
            </div>
            <div class="col-md-12 form-group">
                @Html.LabelFor(m => m.Details, new { @class = "control-label" })
                @Html.TextAreaFor(m => m.Details, new { @class = "form-control input-sm", @placeholder = "Enter Details.." })
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="box-footer">
                    <input type="submit" class="btn btn-info col-sm-offset-4" value="Update" tabindex="2" />
                    <input type="button" class="btn btn-default" value="Cancel" data-dismiss="modal" />
                </div>
            </div>
        </div>
    }
</div>
