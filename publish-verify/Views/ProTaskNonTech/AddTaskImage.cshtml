@model QuickSoft.ViewModel.ViewProTaskViewModel
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4>Add Documents</h4>
    </div>
    <div class="modal-body">
        <form>
            <div class="row">
                <div class="form-group col-md-12">
                    <div>Description</div>
                    <textarea style="width:100%; height:50px" name="desc" id="desc"></textarea>
                    <div class="file-loading">
                        <label class="control-label">Documents</label>
                        <input id="fileupload" class="file" name="fileupload" multiple="multiple" type="file" />
                    </div>

                </div>
                @*<div class="form-group col-md-12 text-center">
                    <input type="button" class="btn btn-default text-center" value="Cancel" data-dismiss="modal" />
                </div>*@
                @*<div class="form-group col-md-12">
                    <h4>Documents</h4>
                    @if (Model.TaskDocuments != null)
                    {
                        if (Model.TaskDocuments.Any())
                        {
                            var Acount = 0;
                            <div class="row">
                                @foreach (var item in Model.TaskDocuments)
                                {
                                    Acount++;
                                    var x = item.FileName.Split('.');
                                    var extn = x[1];
                                    if (extn == "jpg" || extn == "jfif" || extn == "png" || extn == "jpeg")
                                    {
                                        <div class="form-group col-md-2 thumblimit">
                                            <a href="~/uploads/protaskdocuments/@Html.DisplayFor(modelItem => item.FileName)" ID="ImgBtn1" data-lightbox='@Html.DisplayFor(modelItem => @item.FileName)'>
                                                <img alt="@Html.DisplayFor(modelItem => item.FileName)" title="Click to Open" src="~/uploads/protaskdocuments/@Html.DisplayFor(modelItem => item.FileName)" ID="ImgBtn1" class="img-responsive img-fluid thumblimit" />
                                            </a>
                                            <a href="~/uploads/protaskdocuments/@Html.DisplayFor(modelItem => item.FileName)" ID="ImgBtn1" download class="btn btn-primary btn-xs" style="width:100px;"> download</a>

                                        </div>
                                    }
                                    else
                                    {
                                        <div class="form-group col-md-2 thumblimit">
                                            <a href="~/uploads/protaskdocuments/@Html.DisplayFor(modelItem => item.FileName)" data-lightbox='@Html.DisplayFor(modelItem => @item.FileName)' ID="ImgBtn1">
                                                <img width="100" height="100" src="~/uploads/protaskdocuments/docavatar.png" ID="ImgBtn1" class="img-responsive img-fluid thumblimit" />
                                            </a>
                                            <a href="~/uploads/protaskdocuments/@Html.DisplayFor(modelItem => item.FileName)" ID="ImgBtn1" download class="btn btn-primary btn-xs" style="width:100px;"> download</a>

                                        </div>


                                    }
                                }
                            </div>
                        }
                        else
                        {
                            <div>
                                <h3 class="text-danger">
                                    No Attachment Found..!!
                                </h3>
                            </div>
                        }
                    }
                    <hr />
                </div>*@

            </div>
        </form>
    </div>
    @section style{
        @Styles.Render("~/css/bootstrap-fileinput")
        @Styles.Render("~/css/bootstrap-fileinput-rtl")
    }
    @section scripts{
        @Scripts.Render("~/js/jqueryval")
        @Scripts.Render("~/js/bootstrap-fileinput")
        @Scripts.Render("~/js/bootstrap-piexif")
        @Scripts.Render("~/js/bootstrap-purify")
        @Scripts.Render("~/js/bootstrap-sortable")
    }

    <script type="text/javascript">

        $("#fileupload").fileinput({
            uploadExtraData: function () {
                return {
                    description: $("#desc").val()
                };
            },
            uploadAsync: false,
            uploadUrl: '/ProTaskNonTech/ImageAdd/@Model.ProTaskId',
        
            maxFilePreviewSize: 10240,
            showUploadedThumbs: true,
            overwriteInitial: false,
            showUpload: true,
            showCancel: false,
            enableResumableUpload: true,
            autoReplace: true,
            required: true,

            initialPreviewAsData: true,
            initialPreviewFileType: 'image',
            allowedFileExtensions: ["dwg ", "xlsx", "gif", "jpg", "png", "jfif", "jpeg", "doc", "docx", "pdf", "dwg", "pptx", "vsdx",
            'M4A', 'FLAC', 'MP3', 'MP4', 'WAV', 'WMA', 'AAC', 'ogg', 'opus', 'mpg', 'mpeg'],
            fileActionSettings: {
                showRemove: true,
                showUpload: true,
                showZoom: true,
                showCancel: false,
            },
        }).on('filesorted', function (e, params) {
            console.log('File sorted params', params);
        }).on('fileuploaded', function (e, params) {
            console.log('File uploaded params', params);
        }).on('filebeforedelete', function () {
            return new Promise(function (resolve, reject) {
                $.confirm({
                    title: 'Confirmation!',
                    content: 'Are you sure you want to delete this file?',
                    type: 'red',
                    buttons: {
                        ok: {
                            btnClass: 'btn-primary text-white',
                            keys: ['enter'],
                            action: function () {
                                resolve();
                            }
                        },
                        cancel: function () {
                            $.alert('File deletion was aborted! ' + krajeeGetCount('file-6'));
                        }
                    }
                });
            });
        }).on('filedeleted', function () {
            setTimeout(function () {
                $.alert('File deletion was successful! ' + krajeeGetCount('file-6'));
            }, 900);

        });

    </script>
