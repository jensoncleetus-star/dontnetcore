@model QuickSoft.ViewModel.OptionalFieldViewModel
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4>Delete City</h4>
</div>
<div class="modal-body">
    <h3>Are you sure you want to delete this?</h3>
    <hr />
    @using (Html.BeginForm("Delete", "OptionalFieldUpdate", FormMethod.Post, new { id = "deleteform" }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary("", new { @class = "alert alert-danger" })
<input type="hidden" name="id" value="@Model.Ref1" />
    <input type="hidden" name="type" value="@Model.type" />
    <input type="hidden" name="field" value="@Model.field" />
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger col-sm-offset-4" />
            <input type="button" class="btn btn-default" value="Cancel" data-dismiss="modal">
        </div>
    }
</div>