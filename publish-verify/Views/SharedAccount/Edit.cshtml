@model QuickSoft.Models.sharedaccount
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4>Edit Shared Account</h4>
</div>
<div class="modal-body">
    @using (Html.BeginForm("Edit", "SharedAccount", FormMethod.Post, new { id = "editform" }))
    {
        <script type="text/javascript">
            $('form#popupFormColor').removeData("validator");
            $('form#popupFormColor').removeData("unobtrusiveValidation");
            $.validator.unobtrusive.parse($('form#popupFormColor'));
        </script>
        @Html.AntiForgeryToken()
        @Html.ValidationSummary("", new { @class = "alert alert-danger" })
        @Html.HiddenFor(model => model.accsharedid)

<div class="row" style="margin-top:15px;">
    <div class="col-md-12">
        <div class="form-group">
            @Html.Label("Account *", null, new { @class = "control-label temp2" }) 
            @Html.DropDownListFor(model => model.accountid, (SelectList)ViewBag.Account, "Select Account", new { @class = "form-control select2", @required = "required" })

        </div>
    </div>
    <div class="col-md-12">
        <div class="form-group">
            @Html.Label("Material Center *", null, new { @class = "control-label temp2" }) 
            @Html.DropDownListFor(model => model.mcid, (SelectList)ViewBag.MC, "Select Material Center", new { @class = "form-control select2", @required = "required" })

        </div>
    </div>
    <div class="col-md-12">
        <div class="form-group">
            @Html.Label("Percentage *", null, new { @class = "control-label temp2" }) 
            @Html.EditorFor(model => model.percentage, new { htmlAttributes = new { @class = "form-control input-sm" } })

        </div>
    </div>
    @*<div class="col-md-12">
            <div class="form-group">
                @Html.LabelFor(m => m.Type, new { @class = "control-label" }) *
                @Html.TextBoxFor(m => m.Type, new { @class = "form-control", autofocus = "autofocus", @placeholder = "Enter Lead Type", @required = "required" })
            </div>
        </div>*@
</div>
        <div class="row">
            <div class="col-md-12">
                <div class="box-footer">
                    <input type="submit" class="btn btn-info col-sm-offset-4" value="Update" />
                    <input type="button" class="btn btn-default" value="Cancel" data-dismiss="modal" />
                </div>
            </div>
        </div>
    }
</div>
@section scripts{
    @Scripts.Render("~/js/jqueryval")
}
<script>
    $("#accountid").select2({
        placeholder: 'Search Account By Code or name',
        minimumInputLength: 0,
        ajax: {
            url: "/Accounts/AccountsByType",
            dataType: 'json',
            delay: 50,
            data: function (params) {
                return {
                    q: params.term || "",
                    page: params.page || 0,
                    x: ($('#AccGroup').val() != null) ? $('#AccGroup').val() : "All",
                    stt: "All"
                };
            },
            processResults: function (data, params) {
                params.page = params.page || 0;

                return {
                    results: data,
                    pagination: {
                        //more: (params.page * 10) < 50
                        more: true
                    }
                };
            },
            cache: true
        },
    });
    $("#mcid").select2({
        placeholder: 'Search Material Center by Name or Code',
        minimumInputLength: 0,
        ajax: {
            url: "/MC/SearchMC",
            dataType: 'json',
            delay: 50,
            data: function (params) {
                return {
                    q: params.term,
                    page: params.page,
                };
            },
            processResults: function (data, params) {
                params.page = params.page || 1;

                return {
                    results: data
                };
            },
            cache: true
        },
    });
</script>



