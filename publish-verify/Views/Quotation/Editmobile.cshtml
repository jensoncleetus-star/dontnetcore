@model QuickSoft.ViewModel.QuotationViewModel
@{
    ViewBag.Title = "Edit";
    ViewBag.SubTitle = "Edit Quotation";
    ViewBag.Active = "Transactions";
    var TimeOut = @ViewBag.TMOut;
    string IRateCheck = "inactive";
    string check = "";
    string strImage = "";
    string partialconfig = "";
}

@if (User.IsInRole("Dev") || User.IsInRole("ReadOnly Item Rate"))
{
    IRateCheck = "active";
}
@using (Html.BeginForm("UpdateQuotation", "Quotation", FormMethod.Post, new { id = "quotationform", role = "form", enctype = "multipart/form-data" }))
{

    <div class="box box-info">
        <div class="box-header with-border">
            <h3 class="box-title">Edit Quotation <label id="AlertEditable" class="text-red"></label></h3>
            <div class="pull-right">
                @if (@ViewBag.preEntry != 0)
                {
                    if (User.IsInRole("Dev") || User.IsInRole("Edit Quotation"))
                    {
                        <a class="btn bg-black no-border" href="/Quotation/Edit/@ViewBag.preEntry">
                            <i class="fa fa-step-backward"></i>
                        </a>
                    }
                }
                @if (@ViewBag.nxtEntry != 0)
                {
                    if (User.IsInRole("Dev") || User.IsInRole("Edit Quotation"))
                    {
                        <a class="btn bg-black no-border" href="/Quotation/Edit/@ViewBag.nxtEntry">
                            <i class="fa fa-step-forward"></i>
                        </a>
                    }
                }
                else
                {
                    if (User.IsInRole("Dev") || User.IsInRole("Quotation Entry"))
                    {
                        <a class="btn bg-black no-border" href="/Quotation/Create">
                            <i class="fa fa-step-forward"></i>
                        </a>
                    }
                }
                @if (User.IsInRole("Dev") || User.IsInRole("Quotation List"))
                {
                    <a class="btn btn-success no-border" href="/Quotation">
                        <i class="fa fa-fw fa-history"></i>List
                    </a>
                }
            </div>
        </div>
        @Html.AntiForgeryToken()
        @Html.ValidationSummary("", new { @class = "alert alert-danger" })
        <div class="box-body">
            <input type="hidden" id="ROnlyRate" value="@IRateCheck" />
            <input type="hidden" id="SelApprovedBy" />
            <input type="hidden" id="VoucherType" value="Quotation" />
            <div class="row">
                @if (@ViewBag.BusinessType == "Scaffold")
                {
                    <div class="form-group col-sm-2 col-xs-12">
                        @Html.Label("Type", null, new { @class = "control-label" }) *
                        @Html.EnumDropDownListFor(model => model.SaleType, new { @class = "form-control", @onchange = "HType();", @required = "required", data_msg_required = "The Sale type field is required" })
                    </div>
                }
                <div class="col-sm-2 col-xs-12 form-group ">
                    @Html.Label("Quot No", null, new { @class = "control-label" }) *
                    @if (User.IsInRole("Dev") || User.IsInRole("Invoice Edit Permission"))
                    {
                        @Html.EditorFor(model => model.BillNo, new { htmlAttributes = new { @class = "form-control", @required = "required" } })
                    }
                    else
                    {
                        @Html.EditorFor(model => model.BillNo, new { htmlAttributes = new { @class = "form-control", @required = "required", @readonly = "readonly" } })
                    }
                    @Html.HiddenFor(model => model.QuotNo)
                </div>
                <div class="form-group col-sm-2 col-xs-12 no-paddingleft">
                    @Html.Label("Revision:", null, new { @class = "control-label" })
                    <div class="input-group input-group-sm">
                        @Html.EditorFor(model => model.revision, new { htmlAttributes = new { @class = "form-control" } })

                    </div>
                </div>
                <div class="col-sm-2 col-xs-12 form-group">
                    @Html.Label("Date", null, new { @class = "control-label" }) *
                    <div class="input-group date">
                        <div class="input-group-addon"><i class="fa fa-calendar"></i></div>
                        @Html.TextBoxFor(m => m.QuotDate, "{0:dd-MM-yyyy}", new { @class = "form-control datepicker input-sm", @required = "required" })
                    </div>
                </div>
                <div class="col-sm-2 col-xs-12 form-group">
                    @Html.Label("Sale Type", null, new { @class = "control-label" }) *
                    @Html.DropDownListFor(m => m.SalesType, new SelectList(Model.SalesTypes, "Id", "Name"), new { id = "SalesType", @class = "form-control" })
                </div>
                @*<div class="col-sm-2 form-group">
            <label class="control-label" for="Sale_Type">Sale Type</label> *
            <select class="form-control valid" data-val="true" data-val-number="The field SalesType must be a number." data-val-required="The SalesType field is required." id="SalesType" name="SalesType" tabindex="6">
                <option value="1">VAT Item Wise</option>
                <option value="2">Tax Exempt</option>
            </select>
        </div>*@
                <div class="col-sm-2 col-xs-12 form-group">
                    @Html.Label("Sales Executive", null, new { @class = "control-label" })
                    @if (User.IsInRole("Dev") || User.IsInRole("Create Sales Executive"))
                    {
                        <div class="input-group input-group-sm">
                            @Html.DropDownListFor(m => m.QuotCashier, (SelectList)ViewBag.Cashier, "", new { id = "ddlEmployee", @class = "form-control select2" })
                            <span class="input-group-btn">
                                <button type="button" href="/SalesExecutive/AddSalesExecutive" class="modal-edit btn btn-success btn-flat"><i class="fa fa-1x fa-plus-circle"></i></button>
                            </span>
                        </div>
                    }
                    else
                    {
                        @Html.DropDownListFor(m => m.QuotCashier, (SelectList)ViewBag.Cashier, "", new { id = "ddlEmployee", @class = "form-control select2" })
                    }
                </div>
                <div class="col-md-6 col-xs-12 form-group">
                    @Html.Label("Customer Name", null, new { @class = "control-label" }) *
                    @if (User.IsInRole("Dev") || User.IsInRole("Create Customer"))
                    {
                        <div class="input-group input-group-sm">
                            @Html.DropDownListFor(m => m.Customer, (SelectList)ViewBag.Custer, new { id = "ddlCustomer", @class = "form-control select2", @required = "required", @onchange = "GetCustomer()" })
                            @*<span class="input-group-btn">
                        <a href="/Customer/Create" class=" btn btn-success btn-flat" target="_new"><i class="fa fa-1x fa-plus-circle"></i></a>
                    </span>*@
                            @*<span class="input-group-btn">
                                <a href="/Customer/Create" class="btn btn-flat sh-align" target="_new"><i class="fa fa-lg fa-plus-circle" style="padding-right:10px;"></i></a>
                            </span>*@
                        </div>
                    }
                    else
                    {
                        @Html.DropDownListFor(m => m.Customer, (SelectList)ViewBag.Custer, new { id = "ddlCustomer", @class = "form-control select2", @required = "required", @onchange = "GetCustomer()" })
                    }
                </div>
                <div class="col-sm-2 col-xs-12 form-group">
                    @Html.Label("QuotationType", null, new { @class = "control-label" })

                    <div class="input-group input-group-sm">
                        @Html.DropDownListFor(m => m.QuotationType, (SelectList)ViewBag.Quot, "", new { id = "ddlQuote", @class = "form-control select2" })
                        @*<span class="input-group-btn">
                <button type="button" href="/QuotationType/Create" class="modal-create btn btn-success btn-flat"><i class="fa fa-1x fa-plus-circle"></i></button>
                </span>*@
                        @*<span class="input-group-btn">
                    <a href="/QuotationType/Create" class="btn btn-flat sh-align" target="_new"><i class="fa fa-2x fa-plus-circle"></i></a>
                </span>*@
                    </div>

                </div>
            </div>
            <div class="row">


                @if (@ViewBag.EnableCRM == 0)
                {
                    <div class="col-sm-2 col-xs-12 form-group">
                        @if (User.IsInRole("Dev") || User.IsInRole("Create Leads"))
                        {
                            @Html.Label("Leads", null, new { @class = "control-label" })
                            <div class="input-group input-group-sm">

                                @Html.DropDownListFor(m => m.lead, (SelectList)ViewBag.leads, "", new { id = "ddllead", @class = "form-control select2", @onchange = "Getlead()" })



                            </div>
                        }
                        else
                        {
                            @Html.Label("Lead", null, new { @class = "control-label" })
                            @Html.DropDownListFor(m => m.lead, (SelectList)ViewBag.leads, new { id = "ddllead", @class = "form-control select2", @required = "required", @onchange = "Getlead()" })
                        }
                    </div>
                }







                @if (@ViewBag.ProjCheck == 0)
                {
                    <div class="form-group col-sm-2 col-xs-12">
                        @Html.Label("Project", null, new { @class = "control-label" })
                        @if (User.IsInRole("Dev") || User.IsInRole("Create Customer"))
                        {
                        <div class="input-group input-group-sm">
                            @Html.DropDownListFor(m => m.Project, (SelectList)ViewBag.Proj, "--No Project--", new { id = "ddlProject", @class = "form-control select2" })
                            @*<span class="input-group-btn">
                                <a href="/Project/AddProject" class="modal-project btn btn-success btn-flat" title="Add Project"><i class="fa fa-1x fa-plus-circle"></i></a>
                            </span>*@
                            @*<span class="input-group-btn">
                                <a href="/Project/AddProject" class="btn btn-flat sh-align" target="_new"><i class="fa fa-2x fa-plus-circle"></i></a>
                            </span>*@
                        </div>
                        }
                        else
                        {
                            @Html.DropDownListFor(m => m.Project, (SelectList)ViewBag.Proj, "--No Project--", new { id = "ddlProject", @class = "form-control select2" })
                        }
                    </div>
                    <div class="form-group col-sm-2 col-xs-12">
                        @Html.LabelFor(m => m.ProTask, new { @class = "control-label" })
                        @Html.DropDownListFor(m => m.ProTask, (SelectList)ViewBag.getProTask, "", new { id = "ddlProTask", @class = "form-control select2" })
                    </div>
                }


            <div class="form-group col-sm-2 col-xs-12 no-paddingleft">
                @Html.Label("Validity", null, new { @class = "control-label" }) @Html.Label("[Days]", null, new { @class = "labelColor" })
                <div class="input-group input-group-sm">
                    @Html.EditorFor(model => model.QuotValidity, new { htmlAttributes = new { @class = "form-control text-right", @Value = "30" } })
                    @*<span class="input-group-btn">
                        <button type="button" class="btn btn-flat">Days</button>
                    </span>*@
                </div>
            </div>

                <div class="col-sm-2 col-xs-12 form-group">
                    @Html.Label("Status", null, new { @class = "control-label" })
                    <div class="input-group">


                        @Html.DropDownList("qtnstatus", new SelectList(new List<SelectListItem>
                               {
                                   new SelectListItem() {Text = "Open", Value="0"},
                                   new SelectListItem() {Text = "Closed", Value="1"},


                               }, "Value", "Text"), new { @class = "form-control input-sm", @name = "Emirate" })
                    </div>
                </div>

                <div class="col-sm-2 col-xs-12 form-group">
                    @Html.Label("Exp Date", null, new { @class = "control-label" })
                    <div class="input-group date">
                        <div class="input-group-addon"><i class="fa fa-calendar"></i></div>
                        @Html.TextBoxFor(m => m.quotationexpdate, "{0:dd-MM-yyyy}", new { @class = "form-control datepicker input-sm", @required = "required", @onchange = "nodays()" }),
                    </div>
                </div>
                @if (@ViewBag.MLAQuot == 0)
                {
                    <div class="form-group col-sm-2 col-xs-12">
                        @Html.Label("Approved By", null, new { @class = "control-label" })
                        @Html.DropDownList("ddlApprovedBy", (MultiSelectList)ViewBag.ApproveBy, new { @class = "form-control select2", @name = "ddlApprovedBy[]", @multiple = "multiple" })

                    </div>
                }

                @if (@ViewBag.BranchCheck == 0)
                {

                    <div class="form-group col-sm-2 col-xs-12">
                        @Html.Label("Branch", null, new { @class = "control-label" })
                        @Html.DropDownListFor(m => m.Branch, (SelectList)ViewBag.SetBranch, new { id = "ddlBranch", @class = "form-control select2" })
                    </div>

                }
                @if (@ViewBag.BusinessType == "Scaffold")
                {

                    <div id="HireType">
                        <div class="form-group col-sm-2 col-xs-12">
                            @Html.LabelFor(m => m.HireType, new { @class = "control-label" }) *
                            @Html.DropDownListFor(m => m.HireType, (SelectList)ViewBag.HiType, new { id = "ddlHType", @class = "form-control select2", @required = "required", data_msg_required = "The Hire type field is required" })
                        </div>
                        <div class="form-group col-md-2">
                            @Html.Label("From Date", null, new { @class = "control-label" }) *
                            <div class="input-group date">
                                @Html.TextBoxFor(model => model.FromDate, "{0:dd-MM-yyyy}", new { @class = "form-control input-sm", @autocomplete = "off", @onchange = "DateSet();", @required = "required", data_msg_required = "The Start Date field is required" })
                                <div class="input-group-addon">
                                    <i class="fa fa-calendar"></i>
                                </div>
                            </div>
                        </div>
                        <div class="form-group col-md-2">
                            @Html.Label("To Date", null, new { @class = "control-label" }) *
                            <div class="input-group date">
                                @Html.TextBoxFor(model => model.ToDate, "{0:dd-MM-yyyy}", new { @class = "form-control input-sm", @autocomplete = "off", @required = "required", data_msg_required = "The End Date field is required" })
                                <div class="input-group-addon">
                                    <i class="fa fa-calendar"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                }
                @if (@ViewBag.BusinessType == "Scaffold")
                {
                    <div class="form-group col-md-2">
                        @Html.LabelFor(model => model.PaymentTerms, new { @class = "control-label" })
                        @Html.TextBoxFor(model => model.PaymentTerms, new { @class = "form-control", @placeholder = "Payment Terms", @rows = 2 })
                    </div>
                }

                <div class="clearfix"></div>
            </div>
            <div class="row">
                @if (Model.FieldMap.Count > 0)
                {
                    for (int i = 0; i < Model.FieldMap.Count; i++)
                    {
                        var fmap = "";
                        if (Model.FieldMap[i].Field == "Ref1")
                        {
                            fmap = Model.Ref1;
                        }
                        if (Model.FieldMap[i].Field == "Ref2")
                        {
                            fmap = Model.Ref2;
                        }
                        if (Model.FieldMap[i].Field == "Ref3")
                        {
                            fmap = Model.Ref3;
                        }
                        if (Model.FieldMap[i].Field == "Ref4")
                        {
                            fmap = Model.Ref4;
                        }
                        if (Model.FieldMap[i].Field == "Ref5")
                        {
                            fmap = Model.Ref5;
                        }

                        <div class="form-group col-sm-2">
                            <label class="control-label" for="FieldName"> @Model.FieldMap[i].FieldName</label>
                            <input class="form-control input-sm" id="@Model.FieldMap[i].Field" name="@Model.FieldMap[i].Field" type="text" value="@fmap">
                        </div>
                    }
                }
            </div>
            <div>
                <div class="table-responsive">
                    <table class="table table-bordered table-hover table-item" id="normalinvoice">
                        <thead>
                            <tr>
                                <th class="text-center sh-headings-bg">ITEMS</th>

                            </tr>
                        </thead>
                        <tbody id="addinvoiceItem"></tbody>
                    </table>
                    <table class="table table-bordered table-hover">
                        <tr>
                            <th colspan="2"> Total</th>
                        </tr>
                        <tr>
                            <th> Qty</th>
                            <td class="text-right">
                                <label id="ItemQty"></label>
                            </td>
                        </tr>
                        <tr>
                            <th> SubTotal</th>
                            <td class="text-right">
                                <label id="SubTotal"></label>
                            </td>
                        </tr>
                        <tr>
                            <th> Discount</th>
                            <td class="text-right">
                                <label id="ItemDisc"></label>
                            </td>
                        </tr>
                        <tr>
                            <th>Tax</th>
                            <td class="text-right">
                                <label id="total_tax_amount"></label>
                            </td>
                        </tr>
                        <tr>
                            <th>Amount</th>
                            <td class="text-right">
                                <label id="total"></label>
                            </td>
                        </tr>
                    </table>
                </div>
                <div class="row">
                    <div class="col-md-4">
                        @*<div class="form-group no-paddingright">
                                <a id="add_invoice_item" class="btn btn-info" name="add-invoice-item" onclick="addInputField('addinvoiceItem','quot','','0.00','0.00','0');">
                                    <i class="glyphicon glyphicon-plus"></i> Add New Item
                                </a>
                            </div>*@
                        @if (ViewBag.CheckMail == 0)
                        {
                            <div class="form-group">
                                @Html.Label("To", null, new { @class = "control-label" })
                                @Html.TextAreaFor(m => m.CustomerEmail, new { @class = "form-control input-sm", tabindex = 3, rows = 2 })
                                <span class="help-block" style="color:#f39c12;">Enter multiple email separated by space</span>
                            </div>
                            <div class="form-group">
                                @Html.Label("Cc", null, new { @class = "control-label" })
                                @Html.TextAreaFor(m => m.CreatedUserEmail, new { @class = "form-control input-sm", tabindex = 3, rows = 2 })
                                <span class="help-block" style="color:#f39c12;">Enter multiple email separated by space</span>
                            </div>
                        }
                    </div>
                    <div class="col-md-8">
                        <div class="col-md-12">
                            <div class="table-responsive">
                                <table class="table table-bordered table-hover table-item" id="billsundry">
                                    <thead>
                                        <tr>
                                            <th class="text-center sh-headings-bg">BILL SUNDRY</th>

                                        </tr>
                                    </thead>
                                    <tbody id="addbillsundry"></tbody>
                                </table>
                            </div>
                        </div>
                        <div class="form-group col-md-12">
                            @Html.Label("Documents", null, new { @class = "control-label" })
                            <div class="file-loading">
                                <input id="input-24" name="input24[]" type="file" multiple>
                            </div>
                        </div>
                        @foreach (var arr in @ViewBag.image)
                        {
                            check = "\"/uploads/quotationdocument/" + arr.FileName + "\"";
                            strImage += check + ',';

                            partialconfig += "{ key:" + arr.qutid + " }" + ',';

                        }
                        <div class="form-group col-md-4 no-paddingleft">
                            @*@Html.Label("Discount :", null, new { @class = "control-label" })
                                @Html.EditorFor(model => model.QuotDiscount, new { htmlAttributes = new { @id = "Discount", @onchange = "discount_change();", @class = "form-control text-right", @value = "0.00" } })*@
                        </div>
                        <div class="form-group col-md-4 no-padding">
                            @Html.Label("Grand Total :", null, new { @class = "control-label" })
                            @Html.EditorFor(model => model.QuotGrandTotal, new { htmlAttributes = new { @id = "GrandTotal", @class = "form-control text-right", @value = "0.00", @readonly = "readonly" } })
                            <input type="hidden" value="" class="ItemCount" name="ItemCount" id="ItemCount">
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(model => model.Remarks, new { @class = "control-label" })
                            @Html.TextAreaFor(model => model.Remarks, new { @class = "form-control", @placeholder = "Remarks", @rows = 4, @style = "height:100px" })
                        </div>
                        <div class="form-group">
                            @Html.Label("Terms and Conditions", null, new { @class = "control-label" })
                            @Html.TextAreaFor(m => m.TermsCondition, new { @class = "form-control input-sm textarea", @placeholder = "Terms & Conditions", rows = 6, @style = "height:100px" })
                        </div>
                        @if (@ViewBag.PrintLayout == 0)
                        {
                            <div class="form-group col-sm-5">
                                @Html.Label("Print Layout", null, new { @class = "control-label" })
                                @Html.DropDownListFor(m => m.PrintLayout, (SelectList)ViewBag.printlay, new { id = "ddlPrintlayout", @class = "form-control select2" })
                            </div>
                        }
                        <div class="form-group no-padding">
                            <div class="checkbox-inline" style="text-align: left !important;padding-left:30px;">
                                <input class="checkbox" data-val="true" id="hideheader" name="hideheader" type="checkbox" value="true" style="display:none">
                                <label class="control-label" for="hideheader" style="text-align: left !important;">Hide Header In Print</label>
                            </div>
                            <div class="checkbox-inline" style="text-align:left !important;">
                                <input class="checkbox" data-val="true" id="showzeal" name="showzeal" type="checkbox" value="false" style="display:none">
                                <label class="control-label" for="showzeal" style="text-align: left !important;">Show Zeal In Print</label>
                            </div>
                        </div>
                        <div class="col-md-12">

                            <div class="col-md-6">
                                <div class="form-group pull-right">
                                    <button type="submit" id="save" class="btn btn-success">
                                        <span class="glyphicon glyphicon-floppy-disk"></span> Update
                                    </button>
                                    <button type="submit" id="print" class="btn btn-success">
                                        <span class="glyphicon glyphicon-print"></span> Update And Print
                                    </button>
                                    <button type="submit" id="download" class="btn btn-success">
                                        <span class="glyphicon glyphicon-print"></span> Update And Download
                                    </button>
                                    @if (@ViewBag.CheckMail == 0)
                                    {
                                        <button type="submit" id="sendmail" class="btn btn-success">
                                            <span class="glyphicon glyphicon-envelope"></span> Update And Mail
                                        </button>
                                    }

                                    <button type="button" class="btn btn-warning" id="sh-cancel" onclick="@("window.location.href='/Quotation/Index'");">Cancel</button>
                                    
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

}
@{var Modelv = "modal-lg";}
@Html.Partial("../Common/_ModalCreate", @Model)
@Html.Partial("../Common/_ModalEdit", @Model)
@Html.Partial("../Common/_ModeItemCreate")
@Html.Partial("../Common/_ModelProject")
@Html.Partial("../Common/_ModalCustomer")
@*@Html.Partial("_Invoice")*@
@Html.Action("invoice", "InvoiceLayout", new { type = "Quote" })


@section style{
    @Styles.Render("~/css/bootstrap-datepicker")

    @Styles.Render("~/css/wysihtml5")
    @Styles.Render("~/css/bootstrap-fileinput")
    @Styles.Render("~/css/bootstrap-fileinput-rtl")

}
<style>
    @@media only screen and (max-width: 420px) {
        .btn-flat {
            width: 100% !important;
        }

        button[id=sh-btn-dlt2] {
            position: relative;
            top: 17px !important;
            height: 34px !important;
        }

        button[id=sh-btn-dlt] {
            position: relative;
            height: 34px !important;
        }

        input[type=number] {
            text-align: center !important;
        }

        td {
            margin-bottom: 10px !important;
        }

        #sh-btn-add {
            width: 16px !important;
            padding-bottom: 9px !important;
        }

        #itmnote {
            padding-bottom: 15px !important;
            width: 15px !important;
            position: relative !important;
            top: -34px !important;
            right: -16px !important;
        }

        .input-group-sm {
            height: 35px !important;
        }

        .sh-icon1 {
            position: relative !important;
            left: -3px;
            top: 1px;
        }

        .sh-icon2 {
            position: relative !important;
            left: 3px;
            top: 3px;
        }

        .sh-mobi-hide {
            display: none;
        }

        .sh-align {
            right: 1px !important;
            bottom: 7px !important;
            font-size: 17px !important;
        }

        #addinvoiceItem tr {
            text-align: center !important;
            padding: 10px !important;
            border: 3px solid #045de9;
            font-size: 12px;
            letter-spacing: 1px;
        }

        .sh-bottom-align {
            margin-bottom: 10px;
        }
        /*.sh-TaC-align{
            margin-bottom:60px !important;
        }*/
        .sh-plusIcon {
            position: relative;
            top: 3px;
        }

        button[id=save] {
            font-size: 12px;
            width: 47%;
            margin-bottom: 5px;
            padding-top: 7px;
            padding-bottom: 7px;
        }

        button[id=print] {
            width: 47%;
            font-size: 12px;
            padding-top: 7px;
            padding-bottom: 7px;
            margin-bottom: 5px;
        }

        button[id=sendmail] {
            width: 47%;
            font-size: 12px;
            padding-top: 7px;
            padding-bottom: 7px;
            margin-bottom: 5px;
        }

        button[id=sh-cancel] {
            font-size: 13px;
            width: 47%;
            padding-left: 25px;
            padding-right: 25px;
            margin-bottom: 5px;
        }

        .select2-container--default .select2-selection--multiple {
            border-radius: 0px !important;
        }

        .box.box-info .input-group.date {
            border-radius: 0px !important;
        }

        .datepicker {
            border-radius: 0px !important;
        }

        .label-float-down {
            margin-bottom: 0px !important;
        }
    }

    .row {
        letter-spacing: 1px !important;
    }

    .sh-headings-bg {
        background: #eee linear-gradient( #009ffd 17%, #045de9 87%);
        border: none !important;
        color: white !important;
    }

    .sh-headings-bg2 th {
        background: #eee linear-gradient( #009ffd 17%, #045de9 87%);
        border-width: 0 !important;
        color: white !important;
    }

    .box.box-info .select2-container--default .select2-selection--single {
        background-color: #fff;
        border: 1px solid #ccc;
        border-radius: 0px !IMPORTANT;
        height: 34px !important;
    }

    .box.box-info .table th {
        color: #08c;
        min-width: 80px;
    }

    input, button, select {
        border-radius: 0px !important;
    }

    .input-group-btn .sh-align {
        height: 100% !important;
        position: relative;
        right: 4px;
        bottom: 2px;
        font-size: 13px;
    }

    .itemnote {
        height: 200px !important;
    }

    .convert li {
        display: inline-block;
        padding: 5px;
        background-color: ghostwhite;
    }

    .convert ul {
        list-style: none;
    }

    .select2Box {
        width: 400px !important;
    }

    .labelColor {
        background-color: lightblue;
        color: blue;
    }

    .checkboxAlign {
        text-align: left !important;
        padding-left: 30px;
    }

   @* .select2-dropdown{
        width: 154px !important;
    }

    .datepicker {
        width: 248px !important;
    }*@

    .select2-results {
        width: 100px !important;
    }

    .select2-results__options {
        width: auto;
        min-width: 230px !important;
    }

    #select2-item_name_1-results li {
        background-color: lightgray;
        border: 1px solid black;
    }

    #select2-item_name_1-results li:nth-child(2n) {
            background-color: silver;
            border: 1px solid black;
    }

    .select2-container--default #select2-item_name_1-results[aria-selected] {
        background-color: lightgray !important;
        border: 1px solid black;
    }

    #select2-ddlQuote-results, #select2-ddlApprovedBy-results, #select2-ddlCustomer-results, #select2-ddlEmployee-results, #select2-ddlProject-results {
        width: 342px !important;
    }

    .datepicker {
        width: 324px !important;
    }

    .select2-container--default .select2-results__option--highlighted[aria-selected] {
        width: 325px !important;
    }

    .select2-search--dropdown{
        width:232px !important;
    }
  
</style>

@section scripts{
    @Scripts.Render("~/js/jqueryval")
    @Scripts.Render("~/js/bootstrap-datepicker")

    @Scripts.Render("~/js/num2words")
    @Scripts.Render("~/js/JsBarcode")
    @Scripts.Render("~/js/wysihtml5")
    @Scripts.Render("~/js/bootstrap-fileinput")
    <script src="~/Content/js/salesinvoice3.js"></script>

    <script type="text/javascript">
        var getQueryString = function (field, url) {
            var href = url ? url : window.location.href;
            var id = href.substring(href.lastIndexOf('/') + 1);
            return id ? id : null;
        }
        $('.date').datepicker({
            format: 'dd-mm-yyyy',
            autoclose: true,
            allowInputToggle: true,
            startDate: '01-01-2011',
            endDate: '+30d'
        });
        jQuery.validator.methods["date"] = function (value, element) { return true; }

        $(document).ready(function () {

            nodays();
            $('#SaleType option:contains(POS)').remove();
            HType();
            DateSet();
            //CKEDITOR.replace('TermsCondition');
            //$(function () {
            //    $(".textarea").wysihtml5();
            //});
            GetBranch();
            searchItem();
            //Item Category
            ItemCategory();
            ItemBrand();


            CallCustomer();
            CallEmployee();
            CallApprovedBy();
            $("#SelApprovedBy").val($("#ddlApprovedBy").val());

            $("#ddlCustomer").select2({
                placeholder: 'Search Customer by Name or Code',
                minimumInputLength: 0,
                ajax: {
                    url: "/Customer/SearchCustomer",
                    dataType: 'json',
                    delay: 50,
                    data: function (params) {
                        return {
                            q: params.term,
                            page: params.page,
                            y: "quot",
                        };
                    },
                    processResults: function (data, params) {
                        params.page = params.page || 1;

                        return {
                            results: data
                        };
                    },
                    cache: true
                },
            });

            $("#ddlQuote").select2({
                placeholder: 'Search QuotationType',
                minimumInputLength: 0,
                ajax: {
                    url: "/QuotationType/SearchType",
                    dataType: 'json',
                    delay: 50,
                    data: function (params) {
                        return {
                            q: params.term,
                            page: params.page,
                            x: "Select Quotation Type"
                        };
                    },
                    processResults: function (data, params) {
                        params.page = params.page || 1;
                        return {
                            results: data
                        };
                    },
                    cache: true
                },
            });
            function Getlead() {
                var CustomerId = $('#ddlCustomer').val();
                if (CustomerId != null) {
                    $.ajax({
                        url: '/Customer/getlead',
                        type: "GET",
                        dataType: "JSON",
                        data: { CustID: CustomerId },
                        success: function (result) {
                            $("#ddlcustomer").val(result.customerid);
                        }
                    });
                }
            }
            ////bind to project
            var cust = $("#ddlCustomer").val();
            if (cust != null) {
                $("#ddlProject").select2({
                    placeholder: 'Search Project Code Or Name ',
                    minimumInputLength: 0,
                    ajax: {
                        url: "/Project/SearchProjectByCustomer",
                        dataType: 'json',
                        delay: 50,
                        data: function (params) {
                            return {
                                q: params.term,
                                page: params.page,
                                customer: cust,
                                x: "--No Project--"
                            };
                        },
                        processResults: function (data, params) {
                            params.page = params.page || 1;
                            return {
                                results: data
                            };
                        },
                        cache: true
                    },
                    templateResult: projectFormatResult,
                    templateSelection: projectFormatSelection,
                });
            }

            var quotID = getQueryString('');
            if (quotID != null) {
                $.ajax({
                    url: '/Quotation/GetQEItems',
                    dataType: 'json',
                    data: { QuoteEntryID: quotID },
                    cache: true,
                    success: function (data) {
                        $.each($.parseJSON(data), function (i, item) {
                            addrow('addinvoiceItem', 'quot', item.ItemUnit, item.ItemTax, item.ItemTotalAmount, item.ItemQuantity, item.Item, item.ItemCode, item.ItemName, item.ItemUnitPrice, item.ItemSubTotal, item.ItemWithCode, item.ItemTaxAmount, item.ItemDiscount, item.ItemNote, item);
                        });

                        addrow('addinvoiceItem', 'quot', '', '0.00', '0.00', '0');
                        $(function () {

                        });
                        $(function () {
                            $("#TermsCondition").wysihtml5();
                        });
                        $(function () {
                            $("#Remarks").wysihtml5();
                        });
                    }
                });

                $.ajax({
                    url: '/Quotation/GetQtBillSundry',
                    dataType: 'json',
                    data: { quoteID: quotID },
                    cache: true,
                    success: function (data) {
                        $.each(data, function (i, bs) {
                            addbillsundry('addbillsundry', 'quot', bs.BsValue, bs.AmountType, bs.BsAmount, bs.BsType, bs.BSName, bs.BillSundry);
                        });
                        addbillsundry('addbillsundry', 'quot', '0.00', '', '0.00', '');
                        grandtotalcalculation();
                    }
                });
                GetCustomer();
            }

            chkEditEditable("@ViewBag.ChkApp");
        });

        $('body').on('change', '#ddlCustomer', function (e) {
            $('#ddlProject').val(null).trigger('change');
            var cust = $("#ddlCustomer").val();
            if (cust != null) {
                $("#ddlProject").select2({
                    placeholder: 'Search Project Code Or Name ',
                    minimumInputLength: 0,
                    ajax: {
                        url: "/Project/SearchProjectByCustomer",
                        dataType: 'json',
                        delay: 50,
                        data: function (params) {
                            return {
                                q: params.term,
                                page: params.page,
                                customer: cust,
                                x: "--No Project--"
                            };
                        },
                        processResults: function (data, params) {
                            params.page = params.page || 1;
                            return {
                                results: data
                            };
                        },
                        cache: true
                    },
                });
            }
        });
        //sales type change
        $('body').on('change', '#SalesType', function (e) {
            var type = $(this).val();
            ptypechange(type);
        });
        $('body').on('change', '#ddlProject', function (e) {
            $('#ddlProTask').val(null).trigger('change');
            CallTask();
        });




        /* function for Create popup */

        $('span').on('click', '.modal-create', function (e) {
            e.preventDefault();
            var url = $(this).attr('href');
            modalshow(url, '#modal-create');
        });
        $('#modal-create').on('submit', '#createform', function (e) {
            e.preventDefault();
            var url = $('#createform')[0].action;
            var data = $('#createform').serialize();
            createajax(url, data, '#modal-create');
        });

        $('span').on('click', '.modal-edit', function (e) {
            e.preventDefault();
            var url = $(this).attr('href');
            modalshow(url, '#modal-edit');
        });
        $('#modal-edit').on('submit', '#editform', function (e) {
            e.preventDefault();
            var url = $('#editform')[0].action;
            var data = $('#editform').serialize();
            createajax(url, data, '#modal-edit');
        });

        $('span').on('click', '.modal-project', function (e) {
            e.preventDefault();
            var url = $(this).attr('href');
            modalshow(url, '#modal-project');
            //datepickerInit();
        });
        $('#modal-create').on('submit', '#projectform', function (e) {
            e.preventDefault();
            var url = $('#projectform')[0].action;
            var data = $('#projectform').serialize();
            createajax(url, data, '#modal-project');
        });

        function GetCustomer() {
            var CustomerId = $('#ddlCustomer').val();
            if (CustomerId != null) {
                $.ajax({
                    url: '/Customer/GetCustomerEmailById',
                    type: "GET",
                    dataType: "JSON",
                    data: { CustID: CustomerId },
                    success: function (result) {
                        $("#CustomerEmail").val(result.EmailId);
                    }
                });
            }
        }

        $('body').on('change', '#ddlApprovedBy', function (e) {
            $("#SelApprovedBy").val($(this).val());
        });

        $('#quotationform').on('submit', function (e) {
            e.preventDefault();

            if ($("#quotationform").valid()) {
                //var fnval = e.originalEvent.explicitOriginalTarget.id;
                var fnval = $('input[type="submit"], button[type="submit"]', this).filter(':focus').attr('id');
                var parameters = {};
                var QNo = $('#QuotNo').val();
                var BillNo = $('#BillNo').val();
                var CustomerID = $('#ddlCustomer').val();
                var QuotCashierID = $('#ddlEmployee').val();
                var QuotDate = $('#QuotDate').val();
                //pending
                var expdate = $("#quotationexpdate").val();
                var leadsid = $("#ddllead").val();
                var quotationstatus = $("#qtnstatus").val();
                var revision = $("#revision").val();
                var quotationtype = $("#ddlQuote").val();
                var Items = $('#ItemCount').val();
                var ItemQuantity = $('#ItemQty').text();
                var SubTotal = $('#SubTotal').text();
                var Tax = $('#total_tax_amount').text();
                var TotalTax = $('#total_tax_amount').text();
                var Discount = $('#ItemDisc').text();
                var GrandTotal = $('#GrandTotal').val();
                var QuotValidity = $('#QuotValidity').val();
                var tAndc = $('#TermsCondition').val();
                var custMail = $('#CustomerEmail').val();
                var Remarks = $('#Remarks').val();
                var CreatedUserEmail = $('#CreatedUserEmail').val();
                var Branch = $('#ddlBranch').val();
                var Project = $("#ddlProject").val();
                var SaleType = $('#SaleType').val();
                var Start = $('#FromDate').val();
                var End = $('#ToDate').val();
                var HireType = $('#ddlHType').val();
                var quotID = getQueryString('');
                var SalesType = $('#SalesType').val();
                var PaymentTerms = $('#PaymentTerms').val();
                var Task = $('#ddlProTask').val();
                var ApprovedBy = $('#SelApprovedBy').val();

                var Ref1 = "";
                var Ref2 = "";
                var Ref3 = "";
                var Ref4 = "";
                var Ref5 = "";
                if ('@Model.FieldMap.Count' > 0) {
                    Ref1 = $('#Ref1').val();
                    Ref2 = $('#Ref2').val();
                    Ref3 = $('#Ref3').val();
                    Ref4 = $('#Ref4').val();
                    Ref5 = $('#Ref5').val();
                }
                var Layout = $('#ddlPrintlayout').val();
                var HTMLtbl = {
                    getData: function (table) {
                        var data = [];
                        table.find('tr').not(':first').not('.item_').each(function (rowIndex, r) {
                            var cols = [];
                            $(this).find('input,textarea,select').each(function (colIndex, c) {
                                cols.push($(this).val());
                            });
                            data.push(cols);
                        });
                        return data;
                    }
                }
                var HTMLtblbs = {
                    getData: function (table) {
                        var bsData = [];
                        table.find('tr').not(':first').not('.bs_').each(function (rowIndex, r) {
                            var cols = {};
                            $(this).find('input,textarea,select').each(function (colIndex, c) {
                                itid = $(this).attr('data-name').split(' ')[0];
                                itval = ($(this).val() != "") ? $(this).val() : $(this).text();
                                cols[itid] = itval;

                            });
                            bsData.push(cols);
                        });
                        return bsData;
                    }
                }
                var bsData = HTMLtblbs.getData($('#billsundry'));
                parameters.bsmodel = bsData;

                var quotEntry = [CustomerID, QuotCashierID, QuotDate, Items, ItemQuantity,
                TotalTax, Discount, GrandTotal,
                SubTotal, Tax, QuotValidity, tAndc, custMail, CreatedUserEmail, QNo, BillNo, quotID, Remarks,
                Branch, Project, SaleType, Start, End, HireType, SalesType, PaymentTerms, Task, ApprovedBy,
                    Ref1, Ref2, Ref3, Ref4, Ref5, Layout, expdate, leadsid, quotationstatus, revision, quotationtype]


                var data = HTMLtbl.getData($('#normalinvoice'));

                parameters.array = data;
                parameters.quotdata = quotEntry;
                parameters.action = fnval;

                if (fnval == "save" || fnval == "print" || fnval == "download" || (fnval == "sendmail" && custMail != "")) {
                    $.ajax({
                        async: true,
                        cache: false,
                        dataType: "json",
                        type: "POST",
                        contentType: "application/json; charset=utf-8",
                        url: "/Quotation/UpdateQuotation",
                        data: JSON.stringify(parameters),
                        beforeSend: function () {
                            $("button").prop('disabled', true); // disable button
                        },
                        success: function (e) {

                            var fileUpload = $("#input-24").get(0);
                            var files = fileUpload.files;

                            // Create FormData object
                            var fileData = new FormData();

                            // Looping over all files and add it to FormData object
                            for (var i = 0; i < files.length; i++) {
                                fileData.append(files[i].name, files[i]);
                            }

                            fileData.append('id', e.qtnid);


                            $.ajax({
                                url: '/Quotation/UploadFiles',
                                type: "POST",
                                contentType: false, // Not to set any content header
                                processData: false, // Not to process data
                                data: fileData,
                                success: function (result) {
                                    if (fnval == "print") {
                                        PrintInvoice(e, "", "inactive", "Quotation");
                               setTimeout(function () { window.location.href = '/Quotation/Create'; }, '@TimeOut');//{ location.reload(); }, 'TimeOut');

                                    }
                                    else if (fnval == "download") {
                                        if ($('#showzeal').prop('checked') == true) {
                                            var Item = 1;
                                            window.location.href = '/Quotation/Download/' + e.qtnid + "?ItemId=" + Item;
                                        }
                                        else {
                                            window.location.href = '/Quotation/Download/' + e.qtnid;
                                        }

                                        $("button").prop('disabled', false);
                                    }
                                    else {
                                        $('.ajax_response', res_success).text(e.message);
                                        $('.AlertDiv').prepend(res_success);
                                          setTimeout(function () { window.location.href = '/Quotation/Create'; }, '@TimeOut');//{ location.reload(); }, 'TimeOut');

                                    }

                                }

                            });




                            if (e.status == true) {
                                if (fnval == "print") {
                                   //printInvoice(e, "", "inactive", "Quotation");
                                }
                                else {
                                  //$('.ajax_response', res_success).text(e.message);
                                   //('.AlertDiv').prepend(res_success);
                                }
                                // window.location.href = '@Url.Action("Create", "Quotation")';
                             //   setTimeout(function () { window.location.href = '/Quotation/Create'; }, '@TimeOut');//{ location.reload(); }, 'TimeOut');

                            } else {
                                $('.ajax_response', res_danger).text(e.message);
                                $('.AlertDiv').prepend(res_danger);
                                $("button").prop('disabled', false); // enable button
                            }

                        }
                    });
                } else {
                    $('.ajax_response', res_danger).text("Please Enter Email Address");
                    $('.AlertDiv').prepend(res_danger);
                    $('#CustomerEmail').focus();
                }

            }
        });




















        //keep stock change
        function chgkeepstok() {
            //$('#KeepStock').click(function () {
            if ($('#KeepStock').prop('checked') == true) {
                $('.StockSection').show();
            }
            else {
                $('.StockSection').hide();
            }
            //  });
        }
        AddItemPopUp();
        ProjectPopup();
        CustomerPopup();

        //On Modal Create
        $('#modal-create-lg').on('shown.bs.modal', function (e) {
            //Item Category
            ItemCategory();
            ItemBrand();

            Itemtype();
            $('#KeepStock').click(function () {
                clickstock();
            });
        });
        function HType() {
            var Type = $('#SaleType').val();
            var method;
            if (Type == 2) {
                $('#HireType').show();
                method = "Hire";
            }
            else {
                $('#HireType').hide();
                method = "Sale";
            }
            saletypechange(Type, method);

            ////hirerate
            //var Stype = (($('#SaleType').val()) == 1) ? "Sale" : "Hire";

            //$.ajax({
            //    url: '/Quotation/GetHireInvoiceNum',
            //    data: { hiretype: Stype },
            //    type: "POST",
            //    dataType: "JSON",
            //    success: function (Hresult) {
            //        $('#BillNo').val(Hresult);
            //    }
            //});

        }
        //$('body').on('change', '#ddlHType', function (e) {
        //    //var type = $(this).val();
        //    var method = "Hire";
        //    var type = $('#ddlHType').val(); alert(method);
        //    saletypechange(type, method);
        //});
        //function HType() {
        //    var Type = $('#SaleType').val();
        //    if (Type == 2) {
        //        $('#HireType').show();
        //    }
        //    else {
        //        $('#HireType').hide();
        //    }
        //    ptypechange(Type);
        //}
        //function DateSet() {
        //    var ndate = $('#FromDate').val();

        //    $('#ToDate').datepicker({
        //        startDate: ndate,
        //        format: 'dd-mm-yyyy',
        //        autoclose: true,
        //        allowInputToggle: true
        //    });

        //}
    </script>
    <script type="text/javascript">

        function nodays() {


            var date1 = $("#QuotDate").val();
            var dateParts = date1.split("-");
            var dateObject1 = new Date(+dateParts[2], dateParts[1] - 1, +dateParts[0]);
            var date2 = $("#quotationexpdate").val();
            var dateParts2 = date2.split("-");
            var dateObject2 = new Date(+dateParts2[2], dateParts2[1] - 1, +dateParts2[0]);
            //var Difference_In_Time = dateParts2.getTime() - dateObject1.getTime();


            // var Difference_In_Days = Difference_In_Time / (1000 * 3600 * 24);
            const diffTime = Math.abs(dateObject2 - dateObject1);
            const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
            $("#QuotValidity").val(diffDays);

        }

       $("#input-24").fileinput({
            initialPreview: [@Html.Raw(strImage)],
            maxFilePreviewSize: 10240,
            showUploadedThumbs: true,
            overwriteInitial: false,
            showUpload: false,
            enableResumableUpload: true,
            autoReplace: true,
            required: false,
            initialPreviewAsData: true,
            initialPreviewFileType: 'pdf',
            allowedFileExtensions: ["dwg ", "xlsx", "gif", "jpg", "png", "jfif", "jpeg", "doc", "docx", "pdf", "dwg", "pptx", "vsdx",
              'M4A', 'FLAC', 'MP3', 'MP4', 'WAV', 'WMA', 'AAC', 'ogg', 'opus', 'mpg', 'mpeg'],
            fileActionSettings: {
                showRemove: true,
                showUpload: false,
                showZoom: true,
            },
            initialPreviewConfig: [
               @partialconfig
            ],

            deleteUrl: "/ProTask/ImageDelete",
        }).on('filesorted', function (e, params) {
            console.log('File sorted params', params);
        }).on('fileuploaded', function (e, params) {
            console.log('File uploaded params', params);
        }).on('filebeforedelete', function () {
            var aborted = !window.confirm('Are you sure you want to delete this file?');
            if (aborted) {
                //window.alert('File deletion was aborted!');
            };
            return aborted;
        }).on('filedeleted', function () {
            setTimeout(function () {
                //window.alert('File deletion was successful!');
            }, 900);
        });

    </script>
}
