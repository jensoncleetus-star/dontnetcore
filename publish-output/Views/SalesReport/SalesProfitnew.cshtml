@{
    ViewBag.Title = "Search Sales Entry";
    ViewBag.SubTitle = "Sales Report";
    ViewBag.Active = "Reports";
    string actData = "";
}
<style>
    .redclass {
        color: red;
    }
</style>
<div class="box box-default">
    <div class="box-header with-border">
        <h3 class="box-title">Search Sales Entry</h3>
    </div>

    <div class="box-body">
        <form id="search-form">
            @using (Html.BeginForm("Salesprofit", "SalesReport", FormMethod.Post, new { id = "popupForm", target = "_new" }))
            {
                <div class="form-group col-md-1">
                    <input type="hidden" id="cname" value="@ViewBag.CName" />
                    <input type="hidden" id="caddress" value="@ViewBag.CAddress" />
                    @Html.Label("Invoice No", null, new { @class = "control-label" })
                    @Html.TextBox("InvoiceNo", "", new { @class = "form-control input-sm" })
                </div>
                <div class="form-group col-md-1">
                    @Html.Label("Payment Type", null, new { @class = "control-label" })
                    @Html.DropDownList("ddlType", new SelectList(new List<SelectListItem>
                        {
                            new SelectListItem() {Text = "All", Value=""},
                            new SelectListItem() {Text = "Credit Sale", Value="0"},
                            new SelectListItem() {Text = "Cash Sale", Value="1"}
                        }, "Value", "Text"), new { @class = "form-control input-sm", @name = "ddlType", @onchange = "customertype()" })
                </div>
                <div class="form-group col-md-2" id="divpaymethod">
                    @Html.Label("Payment Method", null, new { @class = "control-label" })
                    @Html.DropDownList("ddlPayMethod", (SelectList)ViewBag.PayMethod, "Cash", new { @class = "form-control input-sm" })
                </div>
                <div class="form-group col-md-2" id="creditsale">
                    @Html.Label("Customer", null, new { @class = "control-label" })
                    @Html.DropDownList("ddlCustomer", (SelectList)ViewBag.Customer, new { @class = "form-control input-sm select2" })
                </div>
                <div class="form-group col-md-2" id="execreditsale">
                    @Html.Label("Sales Man", null, new { @class = "control-label" })
                    @Html.DropDownList("ddlSalesMan", (SelectList)ViewBag.SalesMan, new { @class = "form-control input-sm select2" })
                </div>
                <div class="form-group col-md-2" id="execreditsale">
                    @Html.Label("Sales Executive", null, new { @class = "control-label" })
                    @Html.DropDownList("ddlEmployee", (SelectList)ViewBag.SalesExecutive, new { @class = "form-control input-sm select2" })
                </div>
                if (@ViewBag.EnableMCcheck == 0)
                {
                    <div class="form-group col-md-2">
                        @Html.Label("Material Center", null, new { @class = "control-label" })
                        @Html.DropDownList("ddlMC", (SelectList)ViewBag.MC, new { @class = "form-control input-sm" })
                    </div>
                }
                if (@ViewBag.ProjChk == 0)
                {
                    <div class="form-group col-md-2">
                        @Html.Label("Project", null, new { @class = "control-label" })
                        @Html.DropDownList("ddlProject", (SelectList)ViewBag.getProj, "All", new { @class = "form-control input-sm" })
                    </div>
                    <div class="form-group col-md-2">
                        @Html.Label("Task", null, new { @class = "control-label" })
                        @Html.DropDownList("ddlProTask", (SelectList)ViewBag.getProTask, "All", new { @class = "form-control input-sm" })
                    </div>
                }
                @*<div class="form-group col-md-4">
                        @Html.Label("Branch", null, new { @class = "control-label" })
                        @Html.DropDownList("ddlBranch", (SelectList)ViewBag.Branch, new { @class = "form-control input-sm select2" })
                    </div>*@
                <div class="form-group col-md-2" id="searchsource" hidden style="margin-bottom:14px;">
                    @Html.Label("Source", null, new { @class = "control-label" })
                    @Html.DropDownList("ddlsource", (SelectList)ViewBag.Source, "All", new { @class = "form-control" })
                </div>
                <div class="form-group col-md-2">
                    @Html.Label("From", null, new { @class = "control-label" })
                    <div class="input-group date">
                        @Html.TextBox("From", "", "{0:dd-MM-yyyy}", new { @class = "form-control input-sm", @autocomplete = "off" })
                        <div class="input-group-addon">
                            <i class="fa fa-calendar"></i>

                        </div>
                    </div>
                </div>
                <div class="form-group col-md-2">
                    @Html.Label("To", null, new { @class = "control-label" })
                    <div class="input-group date">
                        @Html.TextBox("To", "", "{0:dd-MM-yyyy}", new { @class = "form-control datepicker input-sm", @autocomplete = "off" })
                        <div class="input-group-addon">
                            <i class="fa fa-calendar"></i>
                        </div>
                    </div>
                </div>

                if (@ViewBag.BusinessType == "Scaffold")
                {
                    <div class="form-group col-sm-2">
                        @Html.Label("Type", null, new { @class = "control-label" })
                        @Html.DropDownList("SaleType", (SelectList)ViewBag.Type, new { @class = "form-control input-sm", @onchange = "HType();" })
                    </div>

                    <div id="HireTypeDiv">
                        <div class="form-group col-sm-2">
                            @Html.Label("HireType", null, new { @class = "control-label" })
                            @Html.DropDownList("HireType", (SelectList)ViewBag.HiType, "All", new { id = "ddlHType", @class = "form-control select2" })
                        </div>
                        <div class="form-group col-md-2">
                            @Html.Label("From Date", null, new { @class = "control-label" })
                            <div class="input-group date">
                                @Html.TextBox("FromDate", "", "{0:dd-MM-yyyy}", new { @class = "form-control input-sm", @autocomplete = "off", @onchange = "DateSet();" })
                                <div class="input-group-addon">
                                    <i class="fa fa-calendar"></i>
                                </div>
                            </div>
                        </div>
                        <div class="form-group col-md-2">
                            @Html.Label("To Date", null, new { @class = "control-label" })
                            <div class="input-group date">
                                @Html.TextBox("ToDate", "", "{0:dd-MM-yyyy}", new { @class = "form-control input-sm", @autocomplete = "off" })
                                <div class="input-group-addon">
                                    <i class="fa fa-calendar"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                }
                <div class="form-group col-sm-2">
                    @Html.Label("Location", null, new { @class = "control-label" })
                    @Html.DropDownList("ddllocations", (SelectList)ViewBag.Local, "All", new { id = "ddllocations", @class = "form-control select2" })
                </div>

                <div class="form-group col-sm-2">
                    @Html.Label("technician", null, new { @class = "control-label" })
                    @Html.DropDownList("ddltechnician", (SelectList)ViewBag.Local, "All", new { id = "ddltechnician", @class = "form-control select2" })
                </div>
                <div class="form-group col-md-1">
                    @Html.Label("Hour Rate", null, new { @class = "control-label" })
                    @Html.TextBox("perhourcost", "40", new { @class = "form-control input-sm" })
                </div>
                <div class="form-group col-md-1">
                    @Html.Label("Rate Voucher wise", null, new { @class = "control-label" })
                    @Html.CheckBox("employeehourrate", new { @checked = true })
                </div>
                <div class="form-group col-md-1">
                    @Html.Label("Search", null, new { @class = "control-label" })
                    @Html.TextBox("Searchtext", "", new { @class = "form-control input-sm" })
                </div>
                <div class="box-footer col-md-12">
                    <input type="submit" class="btn btn-info col-sm-offset-5" value="Submit" />
                    <button type="button" class="btn btn-default" onclick="@("window.location.href='/Home/Index'");">Cancel</button>
                </div>
            }
        </form>

    </div>
</div>
<div class="box box-default" id="StockReport">
    <div class="box-header with-border">
        <h3 class="box-title">View Sales Entry Report</h3>
    </div>
    <div class="box-body">
        <input type="hidden" id="cname" value="@ViewBag.CName" />
        <input type="hidden" id="caddress" value="@ViewBag.CAddress" />
        <input type="hidden" id="invoiceno" value="@ViewBag.InvoiceNo" />
        <input type="hidden" id="fromdate" value="@ViewBag.fromdate" />
        <input type="hidden" id="todate" value="@ViewBag.todate" />
        <input type="hidden" id="matcenter" value="@ViewBag.EnableMCcheck" />
        <input type="hidden" id="Scaffold" value="@ViewBag.BusinessType" />

        <div class="box-body">
            <table class="display table table-bordered" id="UserTable">
                <thead>
                    <tr class="bg-gray">
                        <td data-priority="1">S/N</td>
                        <td data-priority="1">Invoice No.</td>
                        <td data-priority="1">Date</td>

                        <td data-priority="1">Customer</td>

                        @*<td data-priority="2">Item Amt</td>*@
                        <td data-priority="1" class="headeralign">Total Amt</td>
                        <td data-priority="1" class="headeralign">Paid Amt</td>
                        <td data-priority="1" class="headeralign">Balance Amt</td>
                        <td data-priority="1" class="headeralign">Invoice<br />Without <br /> Tax</td>
                        <td data-priority="1" class="headeralign">Material<br />Cost</td>
                        @*edited*@
                        <td data-priority="1" class="headeralign">Expense</td>
                        <td data-priority="1" class="headeralign">Labour</td>
                        <td data-priority="1" class="headeralign">Credit Note</td>
                        <td data-priority="1" class="headeralign">Profit</td>
                        @*<td data-priority="3">Last Paid Date</td>*@
                        <td data-priority="1">Sales Executive</td>
                        <td data-priority="1" id="SaleTy">Sales Man</td>
                        <td data-priority="1" id="matcol">Material Center</td>


                        <td data-priority="1" id="SaleTy">contribution</td>
                        <td data-priority="1" id="SaleTy">Loss Reason</td>
                        @*<td>Created By</td>*@
                        @*<td>Branch</td>*@
                    </tr>
                </thead>
                <tbody></tbody>
                <tfoot>
                    <tr class="total">
                        <td>Total:</td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>





                    </tr>
                </tfoot>

            </table>
            <table class="display table table-bordered" id="UserTabletotal">
                <thead>
                    <tr class="bg-gray">
                        <td data-priority="1">S/N</td>
                        <td data-priority="1">Invoice No.</td>
                        <td data-priority="1">Date</td>

                        <td data-priority="1">Customer</td>

                        @*<td data-priority="2">Item Amt</td>*@
                        <td data-priority="1" class="headeralign">Total Amt</td>
                        <td data-priority="1" class="headeralign">Paid Amt</td>
                        <td data-priority="1" class="headeralign">Balance Amt</td>
                        <td data-priority="1" class="headeralign">Invoice<br />Without <br /> Tax</td>
                        <td data-priority="1" class="headeralign">Material<br />Cost</td>
                        @*edited*@
                        <td data-priority="1" class="headeralign">Expense</td>
                        <td data-priority="1" class="headeralign">Labour</td>
                        <td data-priority="1" class="headeralign">Credit Note</td>
                        <td data-priority="1" class="headeralign">Profit</td>
                        @*<td data-priority="3">Last Paid Date</td>*@
                        <td data-priority="1">Sales Executive</td>
                        <td data-priority="1" id="matcol">Material Center</td>

                        <td data-priority="1" id="SaleTy">SaleType</td>
                        <td>contribution</td>
                        @*<td>Created By</td>*@
                        @*<td>Branch</td>*@
                    </tr>
                </thead>
                <tbody>
                    <tr class="total">
                        <td>Total:</td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>





                    </tr>
                </tbody>
                <tfoot>

                </tfoot>

            </table>
        </div>
    </div>
</div>
@Html.Partial("../Common/_ModalCreate")
@section style{
    @Styles.Render("~/css/bootstrap-datepicker")
}
@section Scripts {
    @Scripts.Render("~/js/bootstrap-datepicker")
    <script type="text/javascript">
        $('.date').datepicker({
            format: 'dd-mm-yyyy',
            autoclose: true,
            allowInputToggle: true
        })
        $(document).ready(function () {
            $("#ddlsource").select2({});
            //for hide the data table
            $('#StockReport').hide();
            //end
            $("#divpaymethod").hide();
            $('#HireTypeDiv').hide();
            //$("#SaleType").val(1);
            $('#SaleType option:contains(POS)').remove();

            $("#SaleType").prop('selectedIndex', 0);
            //customertype();
            //customertype();
            //for set default date
            $('#To').datepicker({
                format: 'dd-mm-yyyy',
                autoclose: true,
                allowInputToggle: true
            });
            $('#From').datepicker({
                format: 'dd-mm-yyyy',
                autoclose: true,
                allowInputToggle: true
            });

            var date = new Date();
            var today = new Date(date.getFullYear(), date.getMonth(), date.getDate());
            var priordate = new Date(new Date().setDate(today.getDate() - 0));
            $('#To').datepicker('setDate', today);
            $('#From').datepicker('setDate', priordate);
            //---

            //GetProjectWithAll();
            //GetTaskWithAll();

            ItemSearchable();
            // Material Center
            CallRepCustomer();
            CallRepEmployee();
            $("#ddlMC").select2({
                placeholder: 'Search Material Center by Name or Code',
                minimumInputLength: 0,
                ajax: {
                    url: "/MC/SearchMCUser",
                    dataType: 'json',
                    delay: 50,
                    data: function (params) {
                        return {
                            q: params.term,
                            X: "",
                            page: params.page,

                        };
                    },
                    processResults: function (data, params) {
                        params.page = params.page || 1;

                        return {
                            results: data
                        };
                    },
                    cache: true
                },
            });
            $("#ddlSalesMan,#ddltechnician").select2({
                placeholder: 'Search by Name ',
                minimumInputLength: 0,
                ajax: {
                    url: "/Employee/SearchEmployee",
                    dataType: 'json',
                    delay: 50,
                    data: function (params) {
                        return {
                            q: params.term || "",
                            page: params.page || 0,
                            x: "empty"

                        };
                    },
                    processResults: function (data, params) {
                        params.page = params.page || 0;

                        return {
                            results: data,
                            pagination: {
                                //more: (params.page * 10) < 50
                                more: true
                            }
                        };
                    },
                    cache: true
                },
            });

        });
    $('div').on('click', '.detailsclass', function (e) {
        e.preventDefault();
        var url = $(this).attr('href');
        modalshow(url, '#modal-create');
        //datepickerInit();
    });
    $("#employeehourrate").on("change", function (){
        if ($(this).prop('checked') == true) {
            $("#perhourcost").prop("readonly", false);

        }
        else {
            $("#perhourcost").prop("readonly", true);
        }
    }
    );
        function searchData(data) {

            data.seno = $('#InvoiceNo').val();
            data.customer = $('#ddlCustomer').val();
            data.SalesExecutive = $('#ddlEmployee').val();
            data.SalesMan = $('#ddlSalesMan').val();
            data.fromdate = $('#From').val();
            data.todates = $('#To').val();
            data.type = $('#ddlType').val();
            data.ddMC = $('#ddlMC').val();
            data.satype = $('#SaleType').val();
            data.htype = $('#HireType').val();
            data.hfdate = $('#FromDate').val();
            data.htdate = $('#ToDate').val();
            data.project = $('#ddlProject').val();
            data.task = $('#ddlProTask').val();
            data.srchtxt = $('#Searchtext').val();
            data.perhourcost = $('#perhourcost').val();
            data.source = $('#ddlsource').val();
            data.employeehourrate = $('#employeehourrate').prop("checked");
            data.location = $("#ddllocations").val();
            data.technician =  $("#ddltechnician").val();
        }

    function showdatatable() {

        var oTable = $('#UserTable').DataTable({
            "processing": true, // for show progress bar
            "serverSide": false, // for process server side
            "filter": false, // this is for disable filter (search box)
            "orderMulti": false, // for disable multiple column at once
            // "bStateSave": true,
            "stateSave": true,
            "bDestroy": true,
            "bLengthChange": false,
            responsive: true,
            "paging": true,
            "pageLength": 5000,
            "ordering": true,
            "createdRow": function (row, data, dataIndex) {
                if (data.itemprice.loss == true) {
                   // $(row).addClass('redclass');
                }



            },
            "ajax": {
                "url": '/SalesReport/GetAllSaleprofitnew',
                "data": function (data) {
                    data = searchData(data);
                },
                "type": "POST",
                "datatype": "json",


            },
            "order": [[0, "desc"]],
            "columns": [
                {
                    "data": "SalesEntryId", "name": "SalesEntryId",
                    "render": function (data, type, row, meta) {
                        return meta.row + meta.settings._iDisplayStart + 1;
                    }
                },
                //{ "data": "BillNo", "name": "BillNo" },
                {
                    "name": "BillNo", "data": function (data) {
                              @{
                                  actData += "'<a href=\"/CreditSale/Edit/'+ data.SalesEntryId +'\" target=\"_new\">'+ data.BillNo +'</a>' ";
                              }
                        return @Html.Raw(actData);
                    }
                },
                { "data": "SEDate", "name": "SEDate", "render": function (data) { return convertToDate(data); } },

                {  "name": "Customer",  "data": function (data) {
                              @{
                                  var dataz = "";
                                  if (User.IsInRole("Dev") || User.IsInRole("View Customer"))
                                  {
                                      dataz = "'<a href=\"/Customer/Details/'+ data.CustomerID +'\"  target=\"_new\"  target=\"_new\">'+ data.Customer +'</a>' ";
                                  }
                                  else
                                  {
                                      dataz = "data.Customer";
                                  }
                              }
                              return @Html.Raw(dataz);
                      }
                  },
                //  { "data": "SEGrandTotal", "name": "SEGrandTotal", render: $.fn.dataTable.render.number(",", ".", 2) },
                { "data": "SEGrandTotal", "name": "SEGrandTotal", render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright" },

                //{
                //    "name": "SEGrandTotal", "data": function (result) {

                //        return (result.SEGrandTotal - result.NewExpense);

                //    }, render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright"

                //},



                //{
                //    "name": "SEGrandTotal", "data": function (data) {
                //        var datas = "";
                //        $.each(data.PaymentExpense, function (i, item) {

                //            if (item.Expense != null) {

                //                datas += data.SEGrandTotal - item.Expense;

                //            }
                //            else {

                //                datas += data.SEGrandTotal;
                //            }
                //        });
                //        return datas;
                //    }
                //},

                { "data": "SEPaidAmount", "name": "SEPaidAmount", render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright" },
                { "data": "SEBalanceAmount", "name": "SEBalanceAmount", render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright" },
                { "data": "Credit", "name": "Credit", render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright" },

                //{
                //    "name": "itemprice", "data": function (result) {

                //        return (result.SEGrandTotal - result.SETaxAmount);

                //    }, render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright"

                //},
                {
                    "name": "itemprice", "data": function (result) {
                        if (result.itemprice != null)
                            return (result.itemprice.itemprice);
                        else
                            return 0;

                    }, render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright"

                },

                // { "data": "NewExpense", "name": "NewExpense", render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright" },

                {
                    "name": "NewExpense", "data": function (result) {
                        if (result.salesrtn > 0) {
                            return (result.NewExpense + "<br>" + result.NewExpenselink);
                        }
                        else {
                            return (result.NewExpense + result.discountt + "<br>" + result.NewExpenselink);
                        }

                    }

                },



    {
        "name": "empcoste", "data": function (result) {

            return (result.empcoste);

                    }, render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright"

                },
                {
                    "name": "salesrtn", "data": function (result) {

                        return (result.salesrtn + result.salesrtnnote);

                    }, render: $.fn.dataTable.render.number(",", ".", 2), "sClass": "numberright"

                },
                {
                    "name": "itemprice", "data": function (result) {



                        var profit = 0;
                        if (result.itemprice != 0) {
                            if (result.salesrtn > 0) {
                                if (result.SaleType == 1) {
                                    profit = (result.Credit) - (result.itemprice.itemprice) - (result.NewExpense) - (result.empcoste) - (result.salesrtn) - (result.salesrtnnote);
                                }
                                else {
                                    profit = ((result.SEGrandTotal - result.itemprice.itemprice) - result.SETaxAmount) - (result.NewExpense) - (result.empcoste) - (result.salesrtn) - (result.salesrtnnote);
                                }
                            }
                            else {
                                if (result.SaleType == 1) {
                                    profit = (result.Credit) - (result.itemprice.itemprice) - (result.NewExpense + result.discountt) - (result.empcoste) - (result.salesrtn) - (result.salesrtnnote);
                                }
                                else {
                                    profit = ((result.SEGrandTotal - result.itemprice.itemprice) - result.SETaxAmount) - (result.NewExpense + result.discountt) - (result.empcoste) - (result.salesrtn) - (result.salesrtnnote);
                                }
                            }

                            if (result.SalesStatus == 1)
                                return parseFloat(profit).toFixed(2);
                            else

                                return parseFloat(profit).toFixed(2);

                        }
                        else {
                            return 0;
                        }

                    }
                },




                //  { "data": "lastpaydate", "name": "lastpaydate", "render": function (data) { return convertToDate(data); } },
                { "data": "EmpName", "name": "EmpName" },
                { "data": "SalesMan", "name": "SalesMan" },
                { "data": "MCName", "name": "MCName" },
                {
                    "name": "itemprice", "data": function (result) {



                        var profit = 0;
                        if (result.salesrtn > 0) {
                            if (result.SaleType == 1) {
                                profit = (result.Credit) - (result.itemprice.itemprice) - (result.NewExpense) - (result.empcoste) - (result.salesrtn) - (result.salesrtnnote);
                            }
                            else {
                                profit = ((result.SEGrandTotal - result.itemprice.itemprice) - result.SETaxAmount) - (result.NewExpense) - (result.empcoste) - (result.salesrtn) - (result.salesrtnnote);
                            }
                        }
                        else {
                            if (result.SaleType == 1) {
                                profit = (result.Credit) - (result.itemprice.itemprice) - (result.NewExpense + result.discountt) - (result.empcoste) - (result.salesrtn) - (result.salesrtnnote);
                            }
                            else {
                                profit = ((result.SEGrandTotal - result.itemprice.itemprice) - result.SETaxAmount) - (result.NewExpense + result.discountt) - (result.empcoste) - (result.salesrtn) - (result.salesrtnnote);
                            }
                        }

                        if (result.SalesStatus == 1) {
                            if (result.mycoste == "undefined" || result.mycoste == null)
                                return 0;
                            else
                            return parseFloat((profit * result.mycoste) / result.empcoste).toFixed(2);
                            //return parseFloat(profit).toFixed(2);
                        }
                        else {
                            if (result.mycoste == "undefined" || result.mycoste == null)
                                return 0;
                            else
                            return parseFloat((profit * result.mycoste) / result.empcoste).toFixed(2);

                            //return parseFloat(profit).toFixed(2);
                        }
                    }

                },
                {
                    "name": "itemprice", "data": function (result) {
                        if (result.itemprice != null)
                            return result.itemprice.loss
                        else
                            return 0;
                    }
                }

                // { "data": "User", "name": "User" },
                // { "data": "Branch", "name": "Branch" }
            ],
            buttons: [//'copy', 'csv', 'excel', 'pdf', 'print'
                'colvis',
                {
                    extend: 'print',//
                    title: "",
                    footer: true,
                    filename: function () {
                        return 'Sales-Report';
                    },
                    columnDefs: [
                        { width: 400, targets: 2 }
                    ],
                    exportOptions: {
                        columns: ':visible'
                    },
                    message: function () {
                        var head = '<table class="tblcompact table table-bordered"><thead>' +
                            ' <tr><th class="text-center"><h3 style="margin-top: 0px;">' + $('#cname').val() + '</h3>' + $('#caddress').val() + '<h4>Sales Report</h4></th></tr>' +
                            ' <tr><th><span class="text-left">Invoice No : ' + $('#invoiceno').val() + '</span><span class="text-right pull-right"> From :  ' + $('#fromdate').val() + ' To :' + $('#todate').val() + '</span></th></tr></thead><tbody><tr><td>';
                        return head;
                    },
                    customize: function (win) {
                        // $(win.document.body)
                        //.css('font-size', '10pt')
                        //.css('padding', '1px');
                        $(win.document.body).find('table.tblcompact tbody td').css('padding', '0px');
                        $(win.document.body).find('.dataTable,.dataTable th,.dataTable tr,table.tblcompact tbody td .dataTable td')
                            .css('font-size', '10pt')
                            .css('padding', '5px')
                            .addClass('compact');
                        $(win.document.body).find('.dataTable tr th:nth-child(3)')
                            .css('min-width', '80px');
                        //$(win.document.body).find('.tblcompact tbody td:nth- child(2)')
                        //    .css('padding', '0px');
                        $(win.document.body).append('</td></tr></tbody>')

                    }
                }, 'copy', 'csv', //'excel',
                //Excel
                {
                    extend: 'excel',//
                    title: "",
                    footer: true,
                    filename: function () {
                        return 'Sales-Report';
                    },
                    message: function () {
                        var head = '' + $('#cname').val() + '\n' +
                            '' + $('#caddress').val() + '\n' +
                            ' Sales Report \n' +
                            'Invoice No : ' + $('#invoiceno').val() + '\n' +
                            'From :  ' + $('#fromdate').val() + ' To :' + $('#todate').val() + '';
                        return head;
                    },
                    pageSize: 'A4',
                    exportOptions: {
                        columns: ':visible'
                    },
                },
                {
                    extend: 'pdf',//
                    title: "",
                    footer: true,
                    filename: function () {
                        return 'Sales-Report';
                    },
                    message: function () {
                        var head = '' + $('#cname').val() + '\n' +
                            '' + $('#caddress').val() + '\n' +
                            ' Sales Report \n' +
                            'Invoice No : ' + $('#invoiceno').val() + '\n' +
                            'From :  ' + $('#fromdate').val() + ' To :' + $('#todate').val() + '';
                        return head;
                    },
                    pageSize: 'A4',
                    exportOptions: {
                        columns: ':visible'
                    },
                    customize: function (doc) {
                        doc.pageMargins = [10, 10, 10, 10];
                        doc.defaultStyle.fontSize = 10;
                        doc.styles.tableHeader.fontSize = 10;
                        doc.styles.title.fontSize = 12;
                        // Header font and bg color
                        doc.styles.tableHeader.fillColor = '#d3d3d3';
                        doc.styles.tableHeader.color = 'black';
                        // Remove spaces around page title
                        doc.content[0].text = doc.content[0].text.trim();


                        doc.styles['td:nth-child(2)'] = {
                            width: '100%',
                            'max-width': '100%'
                        }

                        //doc.content[1].table.widths = Array(doc.content[1].table.body[0].length + 1).join('*').split('');

                        //doc.content[1].table.widths = [
                        //'5%',
                        //'7%',
                        //'12%',
                        //'10%',
                        //'10%',
                        //'10%',
                        //'12%',
                        //'12%',
                        //'12%',
                        //'10%'
                        //]

                        //doc['header'] = (function () {
                        //    return {
                        //        columns: [
                        //			{
                        //			    alignment: 'left',
                        //			    italics: true,
                        //			    text: 'dataTables',
                        //			    fontSize: 18,
                        //			    margin: [10, 0]
                        //			},
                        //			{
                        //			    alignment: 'right',
                        //			    fontSize: 14,
                        //			    text: 'Custom PDF export with dataTables'
                        //			}
                        //        ],
                        //        margin: 20
                        //    }
                        //});
                        var objLayout = {};
                        // Horizontal line thickness
                        objLayout['hLineWidth'] = function (i) { return .5; };
                        // Vertikal line thickness
                        objLayout['vLineWidth'] = function (i) { return .5; };
                        //// Horizontal line color
                        //objLayout['hLineColor'] = function (i) { return '#aaa'; };
                        //// Vertical line color
                        //objLayout['vLineColor'] = function (i) { return '#aaa'; };
                        // Left padding of the cell
                        objLayout['paddingLeft'] = function (i) { return 4; };
                        // Right padding of the cell
                        objLayout['paddingRight'] = function (i) { return 4; };
                        // Inject the object in the document
                        doc.content[1].layout = objLayout;
                        // Footer font and bg color
                        doc.styles.tableFooter.fillColor = '#d3d3d3';
                        doc.styles.tableFooter.color = 'black';

                    }
                }

            ],

            sDom: "lBfr<'datatable-scroll't>ip",
            "lengthMenu": [[10, 25, 50, 100, -1], [10, 25, 50, 100, "All"]],
            "footerCallback": function (row, data, start, end, display) {
                var api = this.api(), data;

                // Remove the formatting to get integer data for summation
                var intVal = function (i) {
                    return typeof i === 'string' ?
                        i.replace(/[\$,]/g, '') * 1 :
                        typeof i === 'number' ?
                            i : 0;
                };




                // Total over all pages
                total = api
                    .column(6)
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);

                // Total over this page
                pageTotal = api
                    .column(6, { page: 'current' })
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);

                // Update footer
                $(api.column(6).footer()).html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );
                $("#UserTabletotal tbody tr td:nth-child(7)").html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );

                // Total over all pages
                total = api
                    .column(7)
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);

                // Total over this page
                pageTotal = api
                    .column(7, { page: 'current' })
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);

                // Update footer
                $(api.column(7).footer()).html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );
                $("#UserTabletotal tbody tr td:nth-child(8)").html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );


                // Total over all pages
                total = api
                    .column(8)
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);

                // Total over this page
                pageTotal = api
                    .column(8, { page: 'current' })
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);

                // Update footer
                $(api.column(8).footer()).html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );

                $("#UserTabletotal tbody tr td:nth-child(9)").html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );



                // Total over all pages
                total = api
                    .column(10)
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);

                // Total over this page
                pageTotal = api
                    .column(10, { page: 'current' })
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);
                var api = this.api();
                var response = this.api().ajax.json();
                //$(api.column(10).footer()).html(
                //    response.materialcost
                //);

                //// Update footer
                //if (response) {
                    $(api.column(10).footer()).html(
                        pageTotal.toFixed(2)


                       // response.materialcost.toFixed(2)

                        //'$' + pageTotal + ' ( $' + total + ' total)'
                );
                $("#UserTabletotal tbody tr td:nth-child(11)").html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );

   // }
                    // Total over all pages
                    total = api
                        .column(11)
                        .data()
                        .reduce(function (a, b) {
                            return intVal(a) + intVal(b);
                        }, 0);

                    // Total over this page
                    pageTotal = api
                        .column(11, { page: 'current' })
                        .data()
                        .reduce(function (a, b) {
                            return intVal(a) + intVal(b);
                        }, 0);

                    // Update footer
                    $(api.column(11).footer()).html(
                        pageTotal.toFixed(2)
                        //'$' + pageTotal + ' ( $' + total + ' total)'
                    );

                $("#UserTabletotal tbody tr td:nth-child(12)").html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );

                    // Total over all pages
                    total = api
                        .column(12)
                        .data()
                        .reduce(function (a, b) {
                            return intVal(a) + intVal(b);
                        }, 0);

                    // Total over this page
                    pageTotal = api
                        .column(12, { page: 'current' })
                        .data()
                        .reduce(function (a, b) {
                            return intVal(a) + intVal(b);
                        }, 0);

                    // Update footer
                    $(api.column(12).footer()).html(
                        pageTotal.toFixed(2)
                        //'$' + pageTotal + ' ( $' + total + ' total)'
                    );
                pageTotal = api
                    .column(16, { page: 'current' })
                    .data()
                    .reduce(function (a, b) {
                        return intVal(a) + intVal(b);
                    }, 0);

                // Update footer
                $(api.column(16).footer()).html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );
                $("#UserTabletotal tbody tr td:nth-child(17)").html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );
                $("#UserTabletotal tbody tr td:nth-child(13)").html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );

                    // Total over all pages
                    total = api
                        .column(9)
                        .data()
                        .reduce(function (a, b) {
                            return intVal(a) + intVal(b);
                        }, 0);

                    // Total over this page
                    pageTotal = api
                        .column(9, { page: 'current' })
                        .data()
                        .reduce(function (a, b) {
                            return intVal(a) + intVal(b);
                        }, 0);

                    // Update footer
                    $(api.column(9).footer()).html(
                        pageTotal.toFixed(2)
                        //'$' + pageTotal + ' ( $' + total + ' total)'
                    );

                $("#UserTabletotal tbody tr td:nth-child(10)").html(
                    pageTotal.toFixed(2)
                    //'$' + pageTotal + ' ( $' + total + ' total)'
                );

                    var table = $('#UserTable').DataTable();
                    if (!table.data().any()) {
                        $('#UserTable tfoot').hide();
                    } else {
                        $('#UserTable tfoot').show();
                    }

                }
            ,
            'fnDrawCallback': function () {
                //copyTotalsToLastRow('UserTable');
            }
        });
        var otable2 = $('#UserTabletotal').DataTable({ "responsive": true, "bDestroy": true, });
            //for showing the datatable when click the submit button

            $('#search-form').on('submit', function (e) {

                e.preventDefault();
                oTable.draw();
                $('#SReport').show();
                $('#UserTable').show();



            });
        function copyTotalsToLastRow(tableId) {
            let tableRows = document.getElementById(tableId).rows;
            let rowCount = tableRows.length;
            let cellCount = tableRows[0].cells.length;
            let totalsRowIndex = -1;
            let totalValues = [];
            let i;
            let j;

            for (i = 0; i < rowCount; i++) {
                if (tableRows[i].className.indexOf('total') !== -1) {
                    totalsRowIndex = i;
                    for (j = 0; j < cellCount; j++) {
                        totalValues[j] = tableRows[i].cells[j].innerText;
                    }
                    tableRows[i].classList.remove('total');
                    break;
                }
            }

            if (totalsRowIndex === -1) {
                return;
            }

            for (i = totalsRowIndex; i < (rowCount - 1); i++) {
                for (j = 0; j < cellCount; j++) {
                    tableRows[i].cells[j].innerText
                        = tableRows[i + 1].cells[j].innerText;
                }
            }

            for (i = 0; i < cellCount; i++) {
                tableRows[rowCount - 1].cells[i].innerText = totalValues[i];
            }
            tableRows[rowCount - 1].classList.add('total');

            for (i = 0; i < rowCount; i++) {
                $('#' + tableId).DataTable().row(tableRows[i]).invalidate();
            }
        }

            $('#UserTable tbody').on('click', 'tr', function () {
                if ($(this).hasClass('selected')) {
                    $(this).removeClass('selected');
                }
                else {
                    oTable.$('tr.selected').removeClass('selected');
                    $(this).addClass('selected');
                    $(this).addClass('dataTables_row');
                }
            });

            @*$('#UserTable tbody').on('dblclick', 'tr', function (e) {
                var data = oTable.row(this).data();
                var url = "/SalesReport/custDetails?idcust=" + data.CustomerId + "&fromdate=@ViewBag.fromdate&todate=@ViewBag.todate";
                $(location).attr('href', url);
            });*@
        }

        var flag = 0;
        $('#search-form').on('submit', function (e) {
            e.preventDefault();
            if (flag == 0) {
                showdatatable();
                flag = 0;
            }
            else
                oTable.draw();

            $('#StockReport').show();
        });


        function reload_table() {
            oTable.clear().draw();
            oTable.ajax.reload(null, false);
        }

        $('#UserTable tbody').on('click', 'tr', function () {
            if ($(this).hasClass('selected')) {
                $(this).removeClass('selected');
            }
            else {
                oTable.$('tr.selected').removeClass('selected');
                $(this).addClass('selected');
                $(this).addClass('dataTables_row');
            }
        });


    </script>
}
