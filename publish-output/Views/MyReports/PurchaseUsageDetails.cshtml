<div class="box box-default" id="PReport">
    
    <input type="hidden" id="items" value="@ViewBag.Item" />
    <input type="hidden" id="fromdate" value="@ViewBag.fromdate" />
    <input type="hidden" id="todate" value="@ViewBag.todate" />
    
    <div class="box-header with-border">
        <h3 class="box-title">View Purchase Usage Report</h3>
    </div>
    <div class="box-body">
        <div class="box-body">
            <table class="display table table-bordered" id="UserTable">
                <thead>
                    <tr>
                        <td colspan="3">
                            <span>Item:&nbsp; @ViewBag.ItemName</span>
                         </td>
                        <td colspan="3">
                            From : @Convert.ToString(string.Format("{0:dd-MM-yyyy}", @ViewBag.fromdate))  - @Convert.ToString(string.Format("{0:dd-MM-yyyy}", @ViewBag.todate))
                        </td>
                    </tr>
                    <tr class="bg-gray">
                        <td>S/N</td>
                        <td>Invoice No.</td>
                        <td>Date</td>
                        <td>Supplier</td>
                        <td>Sales Executive</td>
                        <td>Material Requested By</td>
                        
                    </tr>
                </thead>
                <tbody></tbody>
                <tfoot>
                    
                </tfoot>
            </table>
        </div>
    </div>
</div>
@section Scripts {
    <script type="text/javascript">
        $(window).scroll(function (e) {
            var scroll = $(window).scrollTop();


            if (scroll >= 0) {
                $('.treeview-menu').addClass("navbar-hide");
                var i = scroll;
                $(".navbar-hide").css('margin-top', -i);

            } else {
                $(".navbar-hide").css('margin-top', 0);
                $('.treeview-menu').removeClass("navbar-hide");
            }

        });
        $(document).ready(function () {
            var oTable = $('#UserTable').DataTable({
                "processing": true, // for show progress bar
                "serverSide": true, // for process server side
                "filter": false, // this is for disable filter (search box)
                "orderMulti": false, // for disable multiple column at once
                // "bStateSave": true,
                responsive: true,
                "paging": true,
                "pageLength": 5000,
                "bLengthChange": false,
                "ordering": false,
                "ajax": {
                    "url": '/MyReports/GetPurchaseUsageDetails',
                    //"data": {
                        //supplier: getQueryString1('ddlSupplier'), peno: getParameterByName('InvoiceNo'),
                        //SalesExecutive: getParameterByName('ddlEmployee'), type: getParameterByName('ddlType'),
                        //fromdate: getParameterByName('From'), todate: getParameterByName('To'), ddlMC: getParameterByName('ddlMC')
                        //ItemId:
                    //},
                    "data": function (data) {
                        data.Item = $('#items').val();
                        data.fromdate = $('#fromdate').val();
                        data.todate = $('#todate').val();

                    },
                    "type": "POST",
                    "datatype": "json"

                },
                "order": [[0, "desc"]],
                "columns": [
                     {
                         "data": "PurchaseEntryId", "name": "PurchaseEntryId",
                         "render": function (data, type, row, meta) {
                             return meta.row + meta.settings._iDisplayStart + 1;
                         }
                     },
                    //{ "data": "billNo", "name": "billNo" },
                    {
                        "name": "billNo", "data": function (data) {


                            //        rest= '<a href = \"/PurchaseEntry/Edit/' + data.PurchaseEntryId + '\"target=\"_new\">' + data.billNo + '</a> '

                            //    return rest;
                             @{
                                 var rest = "";
                                 if (User.IsInRole("Dev") || User.IsInRole("Edit Purchase Entry"))
                                 {
                                     rest = "<a href = \"/PurchaseEntry/Edit/' + data.PurchaseEntryId + '\"target=\"_new\">' + data.billNo + '</a> ";
                                 }
                                 else
                                 {
                                     rest = "<span>' + data.billNo + '</span>";
                                 }
                             }
                            return '@Html.Raw(rest)';
                        }
                    },
                    { "data": "date", "name": "date", "render": function (data) { return convertToDate(data); } },
                    { "data": "Supplier", "name": "Supplier" },
                    { "data": "FirstName", "name": "FirstName" },
        {
            "name": "MaterialRequestedBy", "data": function (data) {
                var datas = "";
                $.each(data.MaterialRequestedBy, function (i, item) {
                    if (item.Name != null) {
                        if (datas != "") {
                            datas += " ";
                        }
                        datas += item.Name ;

                    }
                });
                return datas;
            }
        },
                ],
                buttons: [//'copy', 'csv', 'excel', 'pdf', 'print'
                'colvis',
                    {
                        extend: 'print',//
                        exportOptions: {
                            columns: ':visible'
                        },
                        title: "",
                        footer: true,
                        filename: function () {
                            return 'Purchase Report';
                        },
                        message: function()
                        {
                            var head = '<table class="tblcompact table table-bordered"><thead>' +
                            ' <tr><th class="text-center"><h3 style="margin-top: 0px;">' + $('#cname').val() + '</h3>' + $('#caddress').val() + '<h4>Purchase Report</h4></th></tr>' +
                            ' <tr><th><span class="text-left">Invoice No : ' + $('#invoiceno').val() + ' </span><span class="text-right pull-right"> From :  ' + $('#fromdate').val() + ' To :' + $('#todate').val() + '</span></th></tr></thead><tbody><tr><td>';
                            return head;
                        },
                        customize: function (win) {
                            // $(win.document.body)
                            //.css('font-size', '10pt')
                            //.css('padding', '1px');
                            $(win.document.body).find('table, th,tr, td ')
                               // .css('border', 'inherit')
                                .css('font-size', '10pt')
                                .css('padding', '5px')
                                .addClass('compact');
                            $(win.document.body).append('</td></tr></tbody>')

                        }
                    }, 'copy', 'csv', //'excel',
                    {
                        extend: 'excel',//
                        title: "",
                        footer: true,
                        filename: function () {
                            return 'Purchase Report';
                        },
                        message: function () {
                            var head = '' + $('#cname').val() + '\n' +
                             '' + $('#caddress').val() + '\n' +
                             ' Purchase Report \n' +
                             'Invoice No : ' + $('#invoiceno').val() + '\n' +
                             'From :  ' + $('#fromdate').val() + ' To :' + $('#todate').val() + '';
                            return head;
                        },
                        pageSize: 'A4',
                        exportOptions: {
                            columns: ':visible'
                        },
                    },
                    {
                        extend: 'pdf',//
                        title: "",
                        footer: true,
                        filename: function () {
                            return 'Purchase Report';
                        },
                        message: function () {
                            var head = '' + $('#cname').val() + '\n' +
                             '' + $('#caddress').val() + '\n' +
                             ' Purchase Report \n' +
                             'Invoice No : ' + $('#invoiceno').val() + '\n' +
                             'From :  ' + $('#fromdate').val() + ' To :' + $('#todate').val() + '';
                            return head;
                        },
                        pageSize: 'A4',
                        exportOptions: {
                            columns: ':visible'
                        },
                        customize: function (doc) {
                            doc.pageMargins = [10, 10, 10, 10];
                            doc.defaultStyle.fontSize = 10;
                            doc.styles.tableHeader.fontSize = 10;
                            doc.styles.title.fontSize = 12;
                            // Header font and bg color
                            doc.styles.tableHeader.fillColor = '#d3d3d3';
                            doc.styles.tableHeader.color = 'black';
                            // Remove spaces around page title
                            doc.content[0].text = doc.content[0].text.trim();

                            doc.styles['td:nth-child(2)'] = {
                                width: '100%',
                                'max-width': '100%'
                            }
                            //doc.content[1].table.widths = Array(doc.content[1].table.body[0].length + 1).join('*').split('');

                          //  doc.content[1].table.widths = [
                          //'10%',
                          //'10%',
                          //'10%',
                          //'10%',
                          //'15%',
                          //'10%',
                          //'10%',
                          //'10%',
                          //'15%'
                          //  ]

                            var objLayout = {};
                            // Horizontal line thickness
                            objLayout['hLineWidth'] = function (i) { return .5; };
                            // Vertikal line thickness
                            objLayout['vLineWidth'] = function (i) { return .5; };
                            //// Horizontal line color
                            //objLayout['hLineColor'] = function (i) { return '#aaa'; };
                            //// Vertical line color
                            //objLayout['vLineColor'] = function (i) { return '#aaa'; };
                            // Left padding of the cell
                            objLayout['paddingLeft'] = function (i) { return 4; };
                            // Right padding of the cell
                            objLayout['paddingRight'] = function (i) { return 4; };
                            // Inject the object in the document
                            doc.content[1].layout = objLayout;
                            // Footer font and bg color
                            doc.styles.tableFooter.fillColor = '#d3d3d3';
                            doc.styles.tableFooter.color = 'black';
                        }


                    },

                ],
                sDom: "lBfr<'datatable-scroll't>ip",
                "lengthMenu": [[10, 25, 50, 100, -1], [10, 25, 50, 100, "All"]],
                "footerCallback": function (row, data, start, end, display) {
                    var api = this.api(), data;

                    // Remove the formatting to get integer data for summation
                    var intVal = function (i) {
                        return typeof i === 'string' ?
                            i.replace(/[\$,]/g, '') * 1 :
                            typeof i === 'number' ?
                            i : 0;
                    };


















                    var table = $('#UserTable').DataTable();
                    if (!table.data().any()) {
                        $('#UserTable tfoot').hide();
                    } else {
                        $('#UserTable tfoot').show();
                    }
                }

            });




            $('#UserTable tbody').on('click', 'tr', function () {
                if ($(this).hasClass('selected')) {
                    $(this).removeClass('selected');
                }
                else {
                    oTable.$('tr.selected').removeClass('selected');
                    $(this).addClass('selected');
                    $(this).addClass('dataTables_row');
                }
            });



        });
    </script>
}